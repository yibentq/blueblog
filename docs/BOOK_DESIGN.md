# 首页"翻书"改版 · 设计规格

> **状态：重启中（2026-09-23），进度不是按步骤数字顺序推进的**。封面（步骤3的一部分）已定稿，
> 之后站长指定了一个跳跃顺序：先做步骤3的最小验证（封面+目录页跨页协不协调），然后**跳过步骤3
> 剩余细节和步骤4**，直接做步骤5（点击目录条目→全文页的翻页动效）。**当前最新进度、已确认的
> 设计决策、原型文件、"仍然跳过没做的部分"清单，都在 `experiments/notebook-spread/README.md`，
> 接手请先读那份**；封面本身的定稿细节在 `experiments/notebook-cover/README.md`；再往下才是
> 本文档第 10 节的旧材质记录。
>
> 下面第 1-9 节是**这次重启之前**（旧的深棕牛皮革方向）的设计记录，包含一些已经不适用的设定
> （比如第 1 节说的封面颜色），原样保留作历史参考，**不要直接当成当前方案去执行**——当前方案
> 以 `experiments/notebook-cover/README.md` + `experiments/notebook-spread/README.md` 为准。

> **旧状态记录（已过时）**：2026-09-22 曾标记"已取消"，见 `docs/HANDOFF.md` 的 v5 节。
> 2026-09-23 站长重新提起，见 v6 节。

> 本文档记录首页从"卡片列表"改造为"翻页手帐"的设定、约束与执行计划。
> 写在这里是为了让任何接手这个任务的人（或另一个 AI 会话）不需要重新和站长对一遍需求。
> **状态：设定阶段已定稿，尚未开始写代码。** 完成后续步骤请在本文件对应小节打勾并补充说明，
> 完工后把关键决策摘要迁移进 `HANDOFF.md`，本文件可以保留作为详细参考。

---

## 0. 背景与目标

首页现在用"一版版卡片列表"展示文章，站长认为这种展示方式无聊。目标是把首页改造成一本可以翻页浏览的"手帐"：
左上角保留 blog blue 签名，下方保留网站信息，主体区域用翻书的方式展示文章目录，点击目录条目进入文章全文页。

**范围限定：仅改动桌面端（电脑版）。移动端保留现有卡片列表，不做改动。**

---

## 1. 核心设定（已与站长确认）

- 这不是"工程师工作日志"，而是**站长自己的日记/手帐**：随手记录一些有趣的照片或文字，语气轻松、非正式。
- **载体必须延续 blog.blue 已有的品牌语言**：深蓝/蓝图配色、白线注记、纸张裁角、连笔签名字体系统（见 `src/utils/brand.js`）。
  手帐是"形式"（翻页、贴照片、写心情），蓝图是"材质和语言"——不能因为做手帐就滑向粉色系/樱花系/马卡龙的通用手帐视觉。
- 站长本人**不是专业或长期的手帐使用者**，是借这个项目学着做。设计基调应该是"认真但不算精通的新手在尝试"，
  不要做成"资深手帐玩家"那种过于娴熟、装饰过度精致的效果——装饰要克制、留白要多、偶尔的不完全对齐是合理的，
  这种"略带笨拙的认真"本身就是真实感来源。
- 标题/心情批注可以用比现有连笔签名更活泼一些的**艺术字体变体**；但目录列表正文、摘录文字必须保证清晰可读，
  不能为了美术效果牺牲阅读体验。**分层用字原则：标题用艺术字，正文/摘录用清晰易读字体。**

## 2. 明确的禁忌（站长直接指出的 AI 常见坑）

这些是硬约束，任何一步产出（原型/代码/文案）都要对照检查：

1. **不要自动加常见的 AI 交互套路**：书本体和便签本身**不应该有 hover 悬浮/高亮特效**。
   现实中的书和便签是静止的物件，不会因为鼠标靠近就悬浮起来。
   如果确实需要交互反馈（比如便签"可以被拿起来"），要用更贴近真实动作的效果（掀开一角、被拖拽），
   而不是数字界面式的 hover 高亮/放大/发光。
2. **拒绝"很新的旧"**：做旧不能是整体套一层滤镜/降低饱和度。真实的旧物磨损是**不均匀、不对称、有使用逻辑的**：
   - 磨损集中在最常被碰到的地方（书角、书脊、最常翻的那几页边缘），不是整体均匀泛黄
   - 材质之间要有"化学反应式"的旧痕（比如金属大头针在纸上留下的锈斑晕染、胶带老化后底下纸张的变色范围比胶带本身大一点、
     翻页多的地方纸张纤维起毛发白）
   - 这种"有因果关系的旧"比"滤镜式的旧"更可信，也更难被一眼看穿是 AI 做的
3. **不要把不属于这个设定的现代美学元素做旧后混进来**：所有装饰元素（胶带样式、便签材质、字体、图片装饰）
   都要能被"这是站长自己的蓝图手帐"这个设定解释，不要因为"好看"就添加跟设定无关的复古元素
   （比如突然出现和蓝图调性无关的花纹、或明显是数码时代产物却强行做旧）。

## 3. 目录页（书的主体）

- 每页目录页放 4–6 篇文章条目，非严格网格对齐，模拟手写记录的随性排布。
- 每条目录包含：标题（可用艺术字体）、日期（手写批注式，斜放）、一两句摘录（清晰易读字体）。
- 纸张要有轻微纹理，磨损遵循第 2 节的"不均匀"原则（书角/书脊先旧）。
- **分册**：按年/季度分册，默认打开最新一册并翻到最新一页。历史册子通过书脊上的可点标签切换。

## 4. 图片装饰系统

图片"贴"在页面上而非规整嵌入，装饰方式按文章 ID 做确定性 hash 分配（同一篇文章每次显示的贴法一致，不随机刷新变化）：

- 胶带贴（角度随机 ±3°~±10°，胶带边缘轻微卷起发黄，中段颜色变化最小）
- 拍立得式白框（底部留白可放一行手写小字图注）
- 大头针/回形针别住（适合截图类竖版图，考虑第 2 节提到的锈斑晕染细节）

**约束**：纯 CSS 实现（伪元素+滤镜+阴影），不额外增加图片请求；不能对图片本身加 hover 特效。

## 5. 标签/搜索 —— 便签系统

- 夹在目录页外围、露出一角，角上要有可辨识的图形/文字暗示内容（不能是纯色三角形让人瞎猜）。
- 样式池（随机分配，避免所有便签长一样，但都要落在蓝图手帐设定内）：方形便签、卡片式索引卡、书签丝带、回形针夹的小纸条。
- 交互：**不用 hover 高亮**，改用更接近"用手碰它"的动作反馈（被拖拽/掀开一角展开抽屉），点击/拖拽后才展开标签云或搜索框。

## 6. 翻页与跳转动效

两级区分清楚：

- **目录页之间翻页**（上一页/下一页）：慢速，模拟真实翻书的物理感（CSS 3D transform），用户主动触发。
- **点击目录条目 → 进入文章全文页**：快速的"哗啦啦翻过一叠纸"效果，营造穿越书页的仪式感，
  控制在 0.6–1 秒内；动画期间异步预取全文内容，动画结束内容同步到位，不能让用户等待感知延迟。

## 7. 技术与 SEO（Claude 自行裁定的部分）

- 底层保持服务端渲染的真实文章列表 + `<a href>` 链接不变，翻书效果是视觉增强层：CSS 3D + 少量 JS 控制状态。
- 无 JS 环境 / 搜索引擎爬虫：直接看到朴素的文章列表兜底（保留现有语义化 HTML 结构）。
- 现有的 sitemap.xml、`/feed.xml`、结构化数据、`/sitemap` 人看的站点地图页**全部保留不动**。
- 标题层级（h1/h2）不因为翻书视觉而错乱。
- 移动端完全保留现有实现，仅在桌面端做媒体查询分支接入新样式。

## 8. 协作方式（站长明确要求，本任务专用）

- **一步一步做，每步先分析再操作，可以慢但不可以求快**。不要一次性把整个功能写完再展示。
- 每一步分析要综合**设计师 / 美术家 / 程序员 / 产品经理**多个角度。
- 设计类判断要说清判断依据，且随时准备被站长的审美反馈推翻重做。

## 1.1 设定纠正（2026-09-22，覆盖第 1 节的部分表述）

第 1 节写"手帐"时留了个隐患：一度把设定具体化成"工程师笔记本"，被站长纠正——**不是工程师人设**，
这本书是站长自己的日记/手帐，用来发布有趣的照片或文字，站长本人也不是专业/长期手帐使用者，是借
blog.blue 学着做。标题/批注允许用比连笔签名更活泼的艺术字体，但正文摘录仍需清晰可读。
不能脱离 blog.blue 域名本身的蓝图/蓝色调性。

同理，"发霉"这个方向后来也被质疑并推翻：发霉传递的是"腐坏/被遗忘"，与"正在被珍惜使用的手帐"矛盾。
见第 10 节，内页痕迹改成了茶渍/水渍/晒痕/包浆这类"用旧"痕迹，而不是霉斑。

## 9. 执行计划与进度

- [x] 步骤 1：设定手帐的"人设"与基调（已定稿，见第 1 节 + 1.1 纠正）
- [x] 步骤 2：材质 / 配色 / 字体 / 装饰元素对照清单（已定稿，见第 10 节的具体实现参数）
- [~] 步骤 3：静态视觉原型（封面+材质定稿，见第10节；跨页最小验证已做一版，
      见 `experiments/notebook-spread/README.md`。**多痕迹页对比、书脊分册标签已于
      2026-09-23 补齐原型**，但都是未经站长确认的判断题版本，书脊标签的点击交互也
      还没接——严格说仍不算"完成"，只是从"没做"推进到"有原型待审"）
- [ ] 步骤 4：目录翻页交互（真实数据接入，服务端兜底列表）——**站长指定顺序中被跳过，尚未开始**
- [~] 步骤 5：点击进入全文的翻页动效（`experiments/notebook-spread/README.md`）——
      **动效原型已做，但用假数据/假延迟触发**，步骤4接入真实数据后需要重新接线；
      站长看过后未给具体反馈即要求先提交，不代表定稿
- [ ] 步骤 6：标签/搜索便签系统
- [ ] 步骤 7：图片装饰系统接入真实上传图片（胶带/拍立得/大头针——大概率也需要用同一套"真实材质"
      思路重做，不能再用 CSS 渐变凑数，这是第 10 节踩坑之后确认的教训）
- [ ] 步骤 8：移动端隔离确认 + SEO/无 JS 兜底验证
- [ ] 步骤 9：整体走查，对照第 2 节禁忌清单逐条检查

## 10. 材质定稿（2026-09-22，取代第 3/4 节里过时的"纯 CSS 装饰"描述）

**核心教训**：第一版原型被站长否决为"崭新的旧"，根源是想用 CSS 渐变/阴影去"描述"旧感，
而不是像 `tools/build-leather.js` 那样用噪声算法真的算出高度图再渲染光影。这版材质全部走后者路线。

**封面（牛皮革，对应实验脚本 `A-antique-leather-cover`）**：
- 细密不规则的皮孔+细纹粒面（Worley 噪声，cell 尺寸调小、crease/domeBias 调低，避免"鳄鱼皮/蛇皮"的规整大颗粒）
- 整体偏暗的深棕色调，几乎不给高光（spec 很低），牛皮是哑光的
- 磨损集中在书角/边缘（中心保留自然光泽摩挲感），边角处叠加深色浸染 + 少量狐斑

**内页（纸张，对应实验脚本 `page-variants.js`）**：
- 高度图改用纤维状 fBm 值噪声（`buildPaperHeight`），不是皮革的颗粒算法——纸不该有"鼓起来的粒"
- 所有内页共用同一份基底配方（seed=502, amp=0.55）+ 同一份边角磨损，保证多页放在一起是"同一本书"而不是拼贴
- 5 种"痕迹故事"变体，按需给不同文章分配（分配机制还没定，见步骤 3 待做）：
  - `P0-clean`：几乎无痕迹，做基线，大多数页应该是这种
  - `P1-tea-ring`：暖褐色单圈茶渍，位置随机但集中在角落，轮廓用随机谐波扭曲（避免"太规则太新"）
  - `P2-water-mark`：更浅更冷的水渍圆环，同样是不规则轮廓
  - `P3-sun-fade`：线性褪色，靠一侧长期被光晒得发白，跟深色渍痕方向相反
  - `P4-handling-patina`：手汗常摸的那个角比整体边角磨损更暗一块
  - 渍痕类的坑：最早用同心圆多层调制做狐斑/渍痕，被站长指出像"真菌感染的同心圆靶心"，
    后改成随机谐波扭曲半径 + 随机偏心，不再是数学意义上的正圆
- 实验脚本目前在 `/home/claude/experiments/`（`aged-textures.js` 封面 + `page-variants.js` 内页 5 变体），
  **还没有移进仓库 `tools/` 目录、也没接入正式渲染流程**，下一步要做这件事


