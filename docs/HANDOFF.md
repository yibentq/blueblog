# blog.blue 进度交接

> 给下一个接手的人/AI。**先读下面"接手须知"**（固定置顶，随状态更新），再按时间倒序读各版本，最新的在最上面。
> 约定：每完成一轮有意义的改动，在最上面加一节，写清"改了什么、为什么这么改、哪里有坑、还差什么"。
> 不要写成 commit log 的复述——commit 里已经有了，这里要写的是**当时的判断依据**。

---

## 接手须知 · 当前状态（更新于 2026-09-24）

### 1. 现在到哪了

- `main` 上任务B已推到 **v20**（v12 卡片缝线 → v13 分页箭头 → v14 烫金线 → v15 皮革贴片 → v16 网格线 → v17 墨线 → v18 引号+红铅笔 → v19 评论框+白墨线 → v20 404 插画；提交信息以 `任务B：` 开头，`git log` 可查）。站长确认过 v13 及之前已部署，**v14 起是否已部署以站长为准**。更早的 `94eb5b2`（v12）说明如下：上一条 `ceb3a1d`（任务 A：贴底 footer + 短内容纵向居中，同样还未部署）、`cdff96b`（首页目录自定义季度下拉，已部署）、`0fd5576`（评论审核重做，已部署）。
- **2026-09-24 站长看了 `cdff96b` 部署效果后当场提出两个新任务，任务 A 已实现（见下），任务 B 还没动手：**

  **任务 A · 首页目录卡片的垂直间距"强制"控制 —— 已实现，`ceb3a1d`，等部署**
  站长原话：「上下线压缩着中间的目录，能不能强制好距离」。诊断见上一版本（间距原本是固定 rem 值，不随视口高度变化）。**选择的方案是诊断里的方向 2（真正的贴底 footer + 居中）**，理由：方向 1（微调固定数值）治标不治本，视口再高一点观感又会打回原样；方向 2 一次性解决所有视口高度下的比例问题，而且是"贴底 footer"这种常见的专业站点布局模式，不是自造概念。
  具体改法：`body` 改成 `min-height:100vh` 的一列 flex，`main` 用 `flex:1 0 auto` 撑满 header/footer 之间的剩余空间并 `flex-shrink:0`（保证绝不会比自己的内容还矮），`main` 内部再 `display:flex;justify-content:center` 让内容在这块空间里居中。效果：内容比可用空间矮的页面（首页目录、短文章、404）会稳稳浮在 header/footer 正中间；内容够长的页面（长文章、评论多的文章页）跟以前的正常文档流表现完全一致，不受影响——这一点是特意验证过的（`flex-shrink:0` + 没有设置任何 `max-height`，所以 main 只会被撑高、永远不会被压缩到比内容矮，不存在 flex 居中常见的"内容溢出被裁切"风险）。
  改动只在 `public/css/style.css` 的 `html`/`body`/`main`/`footer`/`.titleblock` 几条全局规则，后台是独立的 `admin.css` 布局，不受影响。**还差什么：** 这轮改动我在沙箱里只做了 CSS 层面的静态推理校验（括号配对、逐条规则手算下来会不会互相打架），**没有真实浏览器截图**——沙箱没有可用的无头浏览器/Playwright 环境。站长部署后务必实际打开首页、一篇短文章、一篇长文章、404 页，在不同浏览器窗口高度下看一眼观感是否符合预期，有问题随时反馈再迭代。

  **任务 B · 全站视觉"定制化" —— 方向已定，详细设计规格见 `docs/CYANOTYPE_REDESIGN.md`，还没开始写代码**
  站长后续追加反馈：「除了那些在文档里特别提过的地方，还有很多是"假"定制……全部都要定制化」「整体看是喜欢的，
  但总能看到很多 AI 痕迹，以及感觉没有一种看一眼就记住的深刻印象」——这已经不是本节原来写的"逐页盘点通用控件"
  那个小范围任务了，是**全站视觉语言层面的问题**：现在这套"深蓝+烫金+复古衬线+皮革纹理"配色/材质，每一个
  选择的依据都是"显得高级"，没有一个是从"这个博客实际是什么"推出来的。
  **下一个接手者请直接去读 `docs/CYANOTYPE_REDESIGN.md`，不要只看这几行摘要就动手**——那份文档里有：
  站长亲口确认的关键事实（博客内容是"记录有趣图片"、AAAduo 就是站长本人）、具体的两条"AI 痕迹"诊断、
  一个已经在代码里核实过的具体证据（`posts.cover_image` 字段一直存在也一直被 SELECT 出来，但从来没有一个
  前台页面显示过它——图片博客的图片在设计里毫无地位）、提议的新方向（从"工程蓝图"转向"蓝晒摄影工艺
  cyanotype"，两者本来就是站长项目自述里写的同一个概念的两半，只是后半从没真正落地过）、以及分阶段的
  执行顺序。**这个方向目前只在聊天记录里跟站长文字层面对齐过，没有出过任何视觉稿**，第一步是出 1-2 屏
  静态视觉稿给站长看，确认"是不是这个感觉"之后再往下推进，不要一路做到底不回头对齐。

  ⚠️ **2026-09-24 第二轮补充，覆盖上面这条的具体视觉方向**：站长明确说过"不是让你创新，是让你在目前的
  材质上更定制化"——即不要转向蓝晒纸这类新材质，皮革/纸张背景本身站长很喜欢，要动的是叠在这套材质上面
  那些"看着配套、实际是默认套路"的装饰元素。这一轮盘点、第一次尝试（给路径加随机扰动）为什么被站长推翻、
  真正的判断标准是什么——完整过程见新文档 **`docs/UNFORGEABLE_DESIGN.md`**，
  **这份比 CYANOTYPE_REDESIGN.md 更新，下一个接手者先读这份**。还没有定案，下一步做什么见该文档第 5 节。

- 最近几轮改动记录：
  - **v24** 站长反馈：新文章的图片没有相角 → 根因是编辑器插入图片不带空行、图片跟文字挤成一段；渲染器/编辑器/存量文章三处修正（见下方 v24 节）——**部署后要跑一次 `node tools/rerender-posts.js --apply`**
  - **v23** 站长反馈：烫金线只有左边一段是箔、右边退成 1px 细线 → 改成通栏整条烫金（见下方 v23 节）——这是**站长看过 v14 线上效果后的第一条反馈**
  - **v22** 文章正文图片：单独成段的图片渲染成相册照片（四角相角 + FIG. 编号）——**动了内容渲染层，老文章需重新保存**（见下方 v22 节）
  - **v21** 2026-09-24：没有改站点代码——用真实应用 + 真实数据库把 v12–v20 在真实页面上走查了一遍（见下方 v21 节），并补了走查用的种子脚本
  - **v20** 任务B第十项：404 页面手工插画（图掉了）——`docs/UNFORGEABLE_DESIGN.md` 第6节全部条目至此做完（见下方 v20 节）
  - **v19** 任务B第九项：评论输入框换成嵌进皮里的稿纸；评论/站点地图的虚线分隔换成白墨线（见下方 v19 节）
  - **v18** 任务B第七、八项：引用装饰引号手描图形 + blockquote 左线；高亮 `mark` 换成红铅笔排线（见下方 v18 节）
  - **v17** 任务B第六项：hr / 表格分隔线 / 提示框色带换成"同一把尺子在纸上拖出来的墨线"（见下方 v17 节）
  - **v16** 任务B第五项：body 背景线格+针孔换成"同一根尺子压出来"的生成瓦片（见下方 v16 节）
  - **v15** 任务B第四项：标签/按钮/作者徽章/标签计数/置顶丝带统一成"缝边皮革贴片"（见下方 v15 节）
  - **v14** 任务B第三项：烫金分隔线换成箔片转印材质（见下方 v14 节）
  - **v13** 任务B第二项：分页箭头改成 `rosette()` 派生的指针图形（见下方 v13 节）
  - **v12** 任务B第一项：卡片缝线换成真圆柱截面渲染（`94eb5b2`）
  - **v3** 写作系统升级 + 皮革质感视觉（`fb9a4a8`）
  - **v3.1** 卡片质感分层修正（`8f279a7`）
  - **v4** 评论审核重做，文章与评论绑定（`0fd5576`）
  - **v11** 首页放弃两页对开+翻页动效，改成单页"日记目录"（见下方 v11 节）
  - **`cdff96b`** 首页目录自定义季度下拉
  - **`ceb3a1d`** 任务 A：贴底 footer + 短内容纵向居中，见本节上方
- **等站长在线上确认的**：评论收件箱的实际手感（键盘操作、撤销）、卡片质感是否合意、字体（Google Fonts）在真机上的观感。这些我在自动化测试里验证了逻辑，但**手感和观感只有站长能判断**——如果站长有反馈，按反馈迭代，别推翻重来。
- `docs/BOOK_DESIGN.md`（首页视觉层）2026-09-22 曾被否决又于 2026-09-23 重新提起，**这一天内经历了完整的一轮"做复杂→站长要求大改简化"**：封面定稿→跨页原型+动效（v5、v6）→接入真实首页（v7）→视觉走查修可用性问题（v8）→修几何/光影错误（v9）→纸张材质统一（v10）→**站长直接反馈"AI 做的太复杂了，需要大改"，放弃两页对开+3D翻页动效那整套结构，改成单页"日记目录"直接跳转文章**（本文件 v11 节，也是当前最新状态）。**`docs/BOOK_DESIGN.md` 描述的两页对开/装订缝/翻页动效那套设计已经作废**，只作为历史记录保留，不要再照着它改——现在的实现见 v11 节。
- **v11 里全部拿掉的**：跨页对开结构、3D 翻页动效、点目录条目弹窗读全文、书脊分册标签的磨损效果、四种做旧贴图的随机装饰分配、Caveat 手写字体、`/api/notebook/article/:slug`（文章片段）接口、`/api/notebook/article/:slug/view`（片段阅读计数）接口、`src/utils/notebook.js`、`/dev/notebook` 预览路由。`experiments/notebook-cover/`、`experiments/notebook-spread/` 两个原型目录代码还留着（历史参考），但线上已经没有路由指向它们了。
- **v8 里发现、到 v11 都还没处理的**：首页目录里有两篇明显是测试数据的文章（标题类似"h in"、"123"，没有摘要），**需要站长自己登录后台删掉**——这是数据库里的内容，不是代码问题，没有生产库访问权限改不了。

### 2. 项目地图（一句话版）

Express + PostgreSQL + EJS 服务端渲染，无前端框架、无构建步骤。
`src/`（`app.js`、`routes/{public,admin}`、`models/*`、`middleware/*`、`utils/{markdown,brand,watermark,slug}`）·
`views/`（前台页面 + `admin/` 后台 + `partials/`）· `public/`（`css/`、`js/`：`admin.js`＝文章编辑器，`admin-comments.js`＝评论收件箱）· `db/`（`schema.sql`、`migrate.js`、`seed.js`）·
`tools/`（`build-leather.js` 皮革底纹生成、`verify-watermark.js` 水印查证、`e2e/` 集成测试）。

### 3. 怎么和站长协作（他明确说过/一贯的做法）

- **用中文沟通**。给终端命令时**一律用 markdown 代码块**（聊天界面有一键复制按钮）；其余回复以纯文本为主，别堆格式。
- 站长**自己做所有服务器操作**；你负责把实现做到底：写代码 → **自己测过** → 提交 → 推送 → 给他一段可直接粘贴的部署命令。别只丢一个方案让他去实现。
- 设计类任务要**说清判断依据**，并且愿意被推翻：站长的审美反馈是最终依据（v3.1 就是这么来的）。
- 一次给多个任务时，**分开做、分开提交、分开写进本文档**。
- 站长遇到部署报错会把终端输出整段贴回来——按输出诊断，别猜。

### 4. 部署（生产环境）

约定（来自 `deploy.sh`，站长若改过请以实际为准）：项目在 `/var/www/blog-blue`，pm2 进程 `blog-blue`，运行用户 `blogblue`。

```bash
cd /var/www/blog-blue && sudo -u blogblue git pull && sudo -u blogblue npm install && sudo -u blogblue npm run migrate && sudo -u blogblue pm2 restart blog-blue
```

**这次部署实际踩到的两个坑（都已解决，但别再犯）：**

1. **不要用 root 直接操作这个仓库**。用 root 跑过一次 `git pull` 会让 `.git` 里出现 root 拥有的对象文件，之后 `blogblue` 用户 pull 会报 `insufficient permission for adding an object to repository database`。修复：`chown -R blogblue:blogblue /var/www/blog-blue`，此后所有 git/npm/pm2 操作都加 `sudo -u blogblue`。
2. **服务器上的文件必须只通过 `git pull` 更新**。v3 最初是拷贝文件上去的，git 把它们当成"本地未提交的改动"，后续 `git pull` 会拒绝合并（`Your local changes would be overwritten`）。处理方式：先 `tar` 备份 → `git diff <已知提交> --stat` 确认服务器上没有比仓库更新的内容 → `git reset --hard origin/main`。`.gitignore` 已经排除了 `.env`、`uploads/`、`logs/`、`node_modules/`，所以 reset 不会动它们。
3. 忘了跑 `npm run migrate` 不会出事：`src/app.js` 启动时会调用 `commentModel.ensureSchema()` 补上评论表的新列（幂等）。**但新增迁移时也要遵循这个习惯**：schema.sql 写成 `IF NOT EXISTS`，重要的再加启动兜底。

### 5. 凭据与安全

- **绝不要把 token、密钥、密码写进仓库或本文档**（仓库是公开的）。
- 站长曾在聊天里直接贴过 GitHub 细粒度 token 让 AI 推送代码。下一位接手者如需推送，请**向站长要**；推送时用一次性 URL（`git push https://x-access-token:<token>@github.com/...`），不要写进 `git remote`；并提醒站长**用完后撤销/重建**。
- `WATERMARK_SECRET` 一旦改了，已发布图片的防伪暗码全部对不上（详见 v2）。

### 6. 怎么验证改动（不要只靠"看起来对"）

- `tools/e2e/` 有三套可重复的测试（评论模型 SQL、编辑器前端交互、评论收件箱端到端），用一次性的本机 Postgres + Playwright 跑，**说明见 `tools/e2e/README.md`**。这次评论重做就是用它们在真实数据库 + 真实登录/CSRF 上测的，其中还顺带抓出过真问题（前台 `parent_id` 未校验会 500）。
- **（2026-09-24 更新）沙箱里现在可以起真实应用截真实页面**：`apt install postgresql` + Playwright Chromium 都能用，做法和坑见 `tools/e2e/README.md` 末尾"沙箱里的实测记录"，走查数据用 `tools/e2e/seed-showcase.js`。下面/上文里凡是写"沙箱没有无头浏览器"的都已过时。
- **视觉改动要截图看**：起真实模板 + 真实 CSS 用 Playwright 截图。别凭代码想象效果——v3 第一版皮革卡片"代码上没问题"，截图一看就太吵。
- 沙箱环境的小坑：Google Fonts 访问不到（403，回退系统字体，字体观感无法在沙箱确认）；`apt-get install` 前先 `apt-get update`；用 `pkill -f "关键词"` 时，如果同一条命令里也出现了这个关键词，会把自己的 shell 一起杀掉（用 `fuser -k 端口/tcp` 更稳）。

### 7. 已知坑速查（详情在对应版本节）

- **CSP 会拦行内 `style="..."`**（`styleSrc` 只有 self + nonce）。新代码只用 class；老视图里零星的行内 style 在生产环境是不生效的（不影响功能）。—— v4
- 元素自己写了 `display` 会盖掉 `[hidden]`，`admin.css` 末尾有全局兜底。—— v3
- 皮革底纹改配方要**换文件名**（`-v1` → `-v2`），静态资源缓存 7 天。—— v3 / v3.1
- 老文章的 `content_html` 是保存时渲染好的：新 Markdown 语法（高亮、提示框、锚点）要重新保存一次才生效。—— v3
- `/admin/preview` 有独立限流，且被全局限流跳过（否则写作时会把整个后台一起限流）。—— v3
- 中文输入法：所有键盘拦截前先判断 `isComposing` / `keyCode 229`。—— v3 / v4
- 删除一条评论会级联删除它下面的回复（`parent_id ON DELETE CASCADE`）。—— v4

### 8. 待办总表（都是"可选"，站长没排期）

- 文章**修订历史**（每次保存留一份、可回滚）——需要新表 + 迁移
- 代码块**语法高亮**（要加依赖；目前只有 `language-xx` 的 class）、文章**目录（TOC）**（锚点 id 已有，缺前台渲染）
- **读者回复读者**（前台加"回复"按钮，带 `parent_id`；服务端校验已就绪）、新评论**邮件/Webhook 提醒**、**按文章关闭评论**（要给 `posts` 加列）、垃圾评论自动规则、评论搜索
- 水印：老图不补新水印；没有"原图存一份、对外给带水印版"；签名无中文版/单色印刷版（v2）
- 后台目前是纯色 UI（没上皮革质感，后台优先好用）；旧视图里失效的行内 style 可以顺手清理
- 站长确认后的收尾：把"等站长确认"里的项目根据反馈更新成结论

---

## v24 · 2026-09-24 · 站长反馈：新写的文章里图片没有四个角

站长确认过 v22 的相角是有的，但刚写的新文章里图片没有。**这是 v22 的缺陷，不是站长操作问题。**

### 根因

v22 的相册渲染只在\"**整段只有一张图**\"时触发（marked 的 `paragraph` 规则）。但后台编辑器（`public/js/admin.js`）
粘贴/拖入/点工具栏上传图片时，是把 `![](url)` **原样插在光标处、前后不补空行**（图库里的\"插入到光标处\"只补一个换行）。
所以最自然的写法都会让图片跟文字挤成同一个段落：`文字⏎![](x)`、`文字![](x)`、连着贴两张图——这些全部不触发。
我在 v22 只测了\"图片独占段落\"这一种手写情形，没测编辑器实际产生的情形，是我的遗漏。

### 三处修正

1. **渲染器**（`src/utils/markdown.js` 的 `isolateImages`，解析前预处理）：**行首/行尾/独占一行**的图片各自提成独立段落，文字与图片的先后顺序不变。
   行中间的图片仍是行内图；代码围栏、缩进代码、列表/引用/表格/标题行原样不动（10 种摆法逐一核对过，见提交说明）。
2. **编辑器**（`insertImageBlock`）：以后插图片一律前后各补空行（已经有空行就不再补）。**注意不能叫 `insertBlock`**——同一作用域里已经有同名的
   代码块/表格/分隔线插入函数，重名会把它覆盖掉，我第一版就犯了这个错，测试时发现后改名。也不复用它：文件选择器会抢焦点，图片必须按 `lastCaretPos` 插。
3. **存量文章**：`tools/rerender-posts.js`——用当前渲染器把所有文章的 `content_html` 重渲染一遍，**只改 content_html**（不动 updated_at/阅读数/发布时间/content_md）。
   默认 dry-run（只列出哪些文章会变），加 `--apply` 才写；幂等，可重复跑。**这同时解决了 v22 遗留的\"老文章要重新保存才有相角\"。**

### 部署后必须做

```bash
cd /var/www/blog-blue && sudo -u blogblue node tools/rerender-posts.js          # 先看会改哪些
sudo -u blogblue node tools/rerender-posts.js --apply                           # 再真正写入
```

不跑的话：站长新写的那篇（以及所有老文章）的 HTML 是保存时渲染好的，页面上还是没有相角。跑之前建议先 `pg_dump` 备份一下。

### 没做 / 已知

- **图片在行中间**（`前![](x)后`）仍是行内图、没有相角——这是有意保留，没法判断作者是想要相册照片还是行内小图。
- 图片写在**列表/引用里**不会变成相册照片（渲染器只处理正文段落）。
- 验证：真实应用 + 一次性 Postgres，编辑器几种插入位置的填充逻辑在 node 里模拟核对；渲染 10 种图片摆法；真实页面截图看到 4 张图都有相角。**没有在真实浏览器里操作过后台编辑器的粘贴/上传**（沙箱里没有登录态 + 文件选择），所以编辑器那一处是代码审查 + 逻辑模拟，需要站长实际粘贴一张图确认。

---

## v23 · 2026-09-24 · 站长反馈：烫金线要整条都是烫金

站长看了线上的标题栏（截图），原话大意："这个线条前面很好看，但后面都是细线，你也改一下。"
**这是站长对 v14（烫金分隔线）的第一条反馈**，方向明确：前面那段好看，要让整条线都像前面。

### 原因

v14 的设计是**长线只画左边 `clamp(220px, 26vw, 520px)`，转印覆盖率沿长度降到 0**（我当时把"渐隐"做成"箔越往右越薄"）。
所以 1288px 宽的屏幕上只有左边 ~335px 是箔，右边全是标题栏原来的 1px 白细线——这正是站长看到的。
这是我 v14 的设计判断错了："渐隐"在原来是渐变的替代，但站长要的不是渐隐，是整条都有。

### 改了什么

- `tools/build-foil.js`：新增 `'line-wide'` 配方（`fade: 'wide'`），3840×10，**同一个 seed**，所以左端的拉丝和反光带跟 v1 一脉相承。
  覆盖率按**绝对像素**算（前 900px 从完整过渡到"基本是金、偶有斑驳"，然后**一直保持到贴图末端**），不再降到 0。
  试过 plateau 0.37：中段暗缺口太多，读起来又像断线；最后定在 0.31（切片对比过 0/900/1800/2700/3400px 五处）。
- `style.css`：标题栏 `::after` 和页脚 `::before` 都改成 `left:0; right:0`，`background: url(foil-line-wide-v2.png) left center / 3840px 10px no-repeat`——
  **1:1 自然尺寸铺**，不再拉伸（旧版是把 520px 拉到 335px，拉丝纹被压扁）。
- 删除 `foil-line-v1.png`（没有引用了）。`foil-bar-v1.png`（文章标题下的短横）没动。

### 代价 / 没做

- 新图 ~65KB（旧的 9KB），每个访客首次加载一次，之后走 7 天静态缓存。嫌大的话可以缩到 2560 宽 + `repeat-x`，但接缝需要单独处理，没做。
- 屏幕宽于 3840px 时线到贴图末端为止（最后 1.5% 收尾），之后没有箔——极少见，没处理。
- 验证：真实应用，1288 / 2560 / 390 宽的标题栏 + 页脚截图。**没在站长的真机上看过。**
- 如果站长想要"更实"（斑驳更少）或"更旧"（斑驳更多），改 `build-foil.js` 里 `0.11` 那个系数（plateau = 0.2 + 系数）即可。

---

## v22 · 2026-09-24 · 文章正文图片：相角 + 图号（v21 里提出的"最默认的一块"）

上一轮我把这一项作为提议抛给站长（因为它动内容渲染层，不是纯装饰），站长回复"继续"，我按提议的做法做了——**单独提交，不满意可以整个回退**（`git revert` 这一个提交即可，不牵连 v12–v21）。

### 做了什么

- `src/utils/markdown.js`：新增 marked `paragraph` 渲染规则——**整段只有一张图片**时输出
  `<figure class="print"><span class="mount"><img></span><figcaption class="fig-cap">说明</figcaption></figure>`；段落里夹着文字的行内图片**保持原样**。
  说明文字只取 Markdown 图片的 **title**（`![alt](src \"说明\")`），不取 alt：alt 是给读屏用的、常常是文件名，直接印出来会是垃圾。没有 title 就只印图号。
  白名单加了 `figure`/`figcaption`/`span`（三个本来就在 sanitize-html 默认里，这里显式写出来）；试过 title 里塞 `<script>`：会被转义成文本，不会执行。
- `public/img/photo-corner-{tl,tr,bl,br}-v1.svg`：四个手工定的相角（相册里压住照片四角的小三角）。斜边拉了一点弧、两条直角边不等长，靛蓝小牛皮色，斜边一线亮边（皮革切口）；
  **光源永远在左上**——四个方向只镜像形状，阴影方向不镜像（阴影挂在变换外层的 filter 上）。
- `style.css`：`.mount::before` 用四层背景把相角压在照片四角；图号是 CSS 计数器（`FIG. 01`，等宽字体，黄铜色），所以调整图片顺序图号自动跟着变；`counter-reset` 在 `.article-body` 上。
- `admin.css`：预览里不画相角，只保证 `<figure>` 不吃浏览器默认的 40px 缩进 + 图号可见。

### 坑 / 要留意

- **老文章的 `content_html` 是保存时渲染好的**：要重新保存一次才会有相角和图号（跟 v3 的高亮/提示框是同一个限制）。不重新保存的老文章图片还是原来的圆角矩形，不会坏。
- 相角是靛蓝色：在**很深的蓝色照片**上会和照片融在一起看不出来（我拿站点自己的深蓝 og 图测试时就是这样），浅色/中间调照片上清楚。若站长的图普遍偏暗，可以把相角改成小牛皮色，改四个 SVG 里的渐变即可。
- 相角固定 38px，图片很小（<100px 宽）时会显得大；没做响应式缩小。
- 验证：真实应用 + 一次性 Postgres，深色图、浅色图、小图、行内图片混排四种情况的真实页面截图；渲染函数对带 title/不带 title/XSS 试探的输出核对过。**没在真机上看过。**

---

## v21 · 2026-09-24 · 真实页面走查（没有改站点代码）

v12–v20 的每一节都写着"静态页 + 真实 CSS 截图，没连数据库"。这次把缺的补上：一次性 Postgres + `node src/app.js` + `tools/e2e/seed-showcase.js`，
Playwright 截了**真实的首页、文章页（用满所有写作语法）、站点地图、404**（1280 宽；首页另放大看了分页箭头和页头烫金线）。

结果：**所有页面 200，模板正常渲染，没有发现需要修的问题**——
网格线背景、缝线卡片、烫金线、皮革贴片（标签/按钮）、墨线 hr/表格/提示框、引号+blockquote 墨线、mark 排线、代码块、评论区白墨线、稿纸评论框、404 插画同屏协调，
没有互相打架的地方（比如新的 `mask` 规则没有影响别的元素，`.comment:target` 高亮改动后分隔线还在）。

### 仍然没验证的

- **真机/真实字体**：沙箱 Google Fonts 403，正文回退成系统字体，字体观感只有站长能判断；手机真机没看过（只看过窄视口截图）。
- **`mask` 在站长实际用的浏览器里的表现**：已带 `-webkit-` 前缀，现代浏览器应无问题，但没有实测过 Safari。
- 首页目录里的 "h in" / "123" 两篇仍是测试数据（v8 起就在），需要站长在后台删。

### 顺带发现（没改）

- 文章正文里的图片（`.article-body img`）**没有任何设计**——只是圆角矩形。这个站记录的是图片，这是全站现在最"默认"的一块。
  要做的话需要改 `src/utils/markdown.js` 的图片渲染（把 `<img>` 包进带说明的容器，才能加相角/编号），**老文章要重新保存才生效**，
  而且是在动内容层，不是纯装饰，所以没有擅自开工，等站长定方向（见聊天记录）。

---

## v20 · 2026-09-24 · 任务B第十项：404 插画（`docs/UNFORGEABLE_DESIGN.md` 第6节最后一项）

站长让我继续；没有站长对这一项的反馈。**这是第6节列出的最后一项**，做完后第6节没有待办了。

### 画了什么、为什么

第6节说 404 缺的是"一张手工小插画"。这个站记录的是有趣的图片，所以 404 的梗取自图片本身：**图挂丢了**——
墙上只剩一个歪着挂的空相框（玻璃上两道反光说明里面是空的）、图钉孔围出原来那张图的位置（那块墙比周围略亮），
而那张图掉在地上，是蓝晒蓝底、白线的山和日头的拍立得，右下角卷了一角。
颜色只用站点已有的三样：铜（相框，同烫金线/缝线）、蓝晒蓝、线白（同网格线）。

### 做法

`public/img/404-fallen-v1.svg`：**静态手写 SVG，不是运行时/脚本生成的**——第6节对"原创图形"的要求就是像签名一样手工定控制点。
相框四边故意不严格平行、整体旋转 -3.5°，像手扶着画的；不是 `rect` + `rotate` 套出来的。
`views/404.ejs`：500 页面**不显示插画**（"服务器开小差"跟"图掉了"不搭），用 `isServerError` 分支；`<img alt="">`，因为 h1 已经说了意思，插画是装饰。
布局：桌面文字在左、插画在右，窄屏自动折成上下（`.not-found` 改成 flex + wrap）。

### 坑 / 没做

- 错误处理器（`src/app.js`）对所有非 500 的错误（如 400/403）也渲染这个 404 模板并显示"404"——**这是老行为**，这次没改，但如果以后加了别的状态码，需要在模板里区分。
- 验证：抽掉 partials 的模板渲染（404 与 500 两种情形）+ 真实 CSS，Chromium 桌面/手机宽度截图；**没连数据库跑整站、没在真机上看过**。
- 换插画要改文件名（`-v1` → `-v2`），静态资源缓存 7 天。

---

## v19 · 2026-09-24 · 任务B第九项：评论输入框 + 靛蓝底上的分隔线（`docs/UNFORGEABLE_DESIGN.md` 第6节遗留项）

站长让我继续；没有站长对这一项的反馈。

### 评论输入框

原来是 `background: var(--paper)` + 1px 半透明白描边的纯色矩形（HANDOFF v15 已点名是"假定制"）。现在是**嵌在靛蓝皮里的一张纸条**：
皮面在上沿投下内阴影、下沿留一线亮边（跟代码块同一个"凹进去"逻辑）；纸上的线是 v17 那套墨线——
单行输入框底部是一条书写线（聚焦时换成更深的黄铜色版本，同时保留 2px 铜色 outline，键盘可达性不能丢），
评论框是**稿纸横格**（`ink-ruled-v1.png`，560×28，行高 28px，`background-attachment: local` 让线跟着文字滚）。

### 靛蓝底的分隔线：白墨线

评论之间的虚线、站点地图的年份下划线/条目虚线，换成 `ink-rule-light-v1.png` / `ink-rule-lightfaint-v1.png`。
判断依据：**蓝图本来就是蓝底白线**，所以靛蓝皮上的线用"白墨"，而不是把纸上的墨线硬套上去；仍是同一把尺子，
但压在不同的位置（`shift` 不同），所以两处的线不会长得一样。都由 `tools/build-ink.js` 生成（`renderRule` 现在支持 `h`/`cy` 参数）。

### 坑 / 没做

- `.comment:target` 的高亮从 `background` 改成 `background-color`，否则会连分隔线一起盖掉；`.comment.reply` 用 `background-image: none` 去掉线（原来是透明 border）。
- 页头/页脚的分隔线本来就是 v14 的烫金线，没动。
- 验证：静态页 + 真实 CSS，Chromium 2x 截图（评论、回复、表单聚焦态、站点地图）；**没连数据库、没在真机上看过**。
- 第6节现在只剩 **404 插画**（要一张手工小插画，单独排期）。

---

## v18 · 2026-09-24 · 任务B第七、八项：引用装饰引号 + 高亮 mark（`docs/UNFORGEABLE_DESIGN.md` 第6节）

站长让我继续；没有站长对这两项的反馈。**两项分开提交**（先引号后 mark）。

### 引用装饰引号（`public/img/quote-mark-v1.svg`）

第6节归到"原创图形"：工作量小，手工画一个笔触引号，固定复用，不在运行时生成。原来是字体里的 `"` 字符（Fraunces 2.6rem，铜色半透明）。
现在是手描的两笔 SVG：**两笔不是复制关系**（第二笔略小、略偏、笔尾更短），所以不是同一个形状贴两次。`::before` 用 `background: url(svg)`，`opacity: .55`。
顺手把 blockquote 的 `border-left: 3px solid` 也换成 v17 的 `ink-bar-v1.png` mask（挂在 `::after`），因为它跟提示框色带是同一类"默认色块"。

### 高亮 mark（`tools/build-pencil.js`，`npm run pencil`）

第6节说 mark "该换的是隐喻本身"。原来是荧光笔式的 `linear-gradient(transparent 58%, 铜色 58%)`；现在是**红铅笔排线**（制图/校对划重点）：
一组倾斜约 58° 的短笔画，起落笔位置不齐（上沿是毛的）、两头轻中间重、角度和间距略抖，纸纤维噪声决定铅笔只蹭到凸处（粒状）。
输出 288×14 的 `pencil-hatch-v1.png`（红色取 `--danger`），CSS `repeat-x` 铺在 mark 下缘，`box-decoration-break: clone` 保证折行时每行都有。

### 坑 / 没做

- 第一版排线太稀，读起来像一排虚线而不是"涂了一片"，加密后才对。**若觉得太重/太淡**，改 `build-pencil.js` 的 `N`（笔画数）和 `pr`（压力）即可，别改成渐变。
- 没做红蓝双色（红蓝铅笔另一头是蓝）：只用了红，蓝色留给以后有"另一种标注"需求时。
- 验证：静态页 + 真实 CSS，Chromium 3x 截图（含跨行 mark）；引号/blockquote 同样。**没连数据库、没在真机上看过**。
- 换配方要改文件名（`-v1` → `-v2`）。
- 第6节还剩：评论输入框（仍是纯色矩形）、站点地图页/评论区分隔线（在靛蓝皮上，需亮线版）、404 插画（单独排期）。

---

## v17 · 2026-09-24 · 任务B第六项：墨线（hr / 表格分隔线 / 提示框色带，`docs/UNFORGEABLE_DESIGN.md` 第6节 callout·hr·表格那一行）

站长让我继续、不提意见；这一项是我按第6节"网格线的墨水+纸材质做出来后，直接套小尺度版本，不单独立项"自己选的，
**没有站长对这一项的反馈**。

原来：`hr` 是 1px 虚线，表格是 1px 实线上下沿 + 虚线行间，提示框左侧是 4px 纯色块——都是默认套路。
现在三处是同一样东西：**同一把尺子（build-grid.js 那根）在米黄纸上拖出来的墨线**。

### 做法（`tools/build-ink.js`，`npm run ink`）

- **同一根尺子是字面意义的**：`RULER` 用和 build-grid.js 相同的种子、相同的抽取顺序，所以正文里线的弯法和背景线格是同一根尺子的边。
  （**改 build-grid.js 里 RULER 的种子或抽取顺序，这里要同步改**，否则两边不再是同一把尺子。）
- 卡片是纸不是皮，所以不是"凹槽+光照"，而是墨：压力沿线缓慢起伏（线宽/墨深跟着变）、纸纤维噪声决定个别位置墨挂不住（细碎漏墨）、
  压力大处有一点洇边；hr 上每 140px（= 网格主线间距）有一个扎针留下的墨点，中心透纸色。
- 输出四张 alpha 瓦片（横向周期 560px，整数谐波，接缝已数值核对）：`ink-rule-v1.png`（hr，CSS 当 `mask` 用，颜色由 `background` 决定）、
  `ink-rule-brass-v1.png` / `ink-rule-faint-v1.png`（表格上下沿+表头下沿 / 行间，预染黄铜色，因为 `<table>` 挂不了伪元素）、
  `ink-bar-v1.png`（提示框色带，竖向 8×560，同样当 `mask`，四种色带颜色沿用原来各变体的 `background`）。
- 表格行间线画在 `tbody tr` 上而不是每个 `td`：避免各格子起点处相位断开。

### 坑 / 没做

- `mask` 已带 `-webkit-` 前缀；不支持 mask 的老浏览器会看到一条纯色 8px 粗带（hr/色带），现代浏览器都支持，没做降级。
- 站点地图页（`.sitemap-list`，靛蓝底上的虚线分隔）**没动**——它在皮上不在纸上，需要另一套（亮线）版本；`comment` 分隔线同理。
- 验证：静态页 + 真实 CSS，Chromium 2x/4x 截图（hr、表格、四种提示框）；瓦片接缝数值核对；**没连数据库、没在真机上看过**。
  若站长觉得线"太淡/看不出来"，加大 `renderRule` 的 `hw` 与压力项即可，别改成逐点随机。
- 换配方要改文件名（`-v1` → `-v2`），静态资源缓存 7 天。

---

## v16 · 2026-09-24 · 任务B第五项：网格线 + 交点针孔（`docs/UNFORGEABLE_DESIGN.md` 第6节"网格线 + 针孔"项）

站长让我继续、不提意见，没有站长对这一项的反馈。**这是全站背景，每个页面都会看到，改动面最大，
部署后请务必第一时间看一眼。**

原来 body 背景是 5 层 CSS 渐变：每 28px 辅线、每 140px 主线、每个交点同一个圆形针孔——数学格子。
换成 `tools/build-grid.js`（`npm run grid`）生成的 560×560 无缝瓦片 `public/img/grid-navy-v1.webp`
（约 100KB，叠在 leather-navy 之上）。CSS 只是把 body 的背景层从 5 条渐变换成这一张图。

### 判断依据（为什么不是"加抖动"）

UNFORGEABLE_DESIGN 第3节已经否掉了"给每个顶点各自加随机偏移"。这版的核心是**瑕疵要有共同成因，所以彼此相关**：
- **同一根尺子**：所有横线、竖线共用一个固定的、极轻微弯曲的轮廓 `bend(u)`（周期 = 瓦片宽，几个整数谐波叠加）。
  每条线 = 尺子放在不同位置（shift）+ 一点点不同倾角（tilt）压一遍，所以每条线的弯曲"长得像"同一个东西的不同截取。
- **压力不均**：每条线的压印深度沿线缓慢起伏（平滑曲线，不是逐点噪声）。
- **针孔跟着线走**：针孔画在"这条实际压出来的横线"和"这条竖线"真正相交的位置，孔径/深度由该点种子决定，
  并且带一圈被顶起的亮边。
- 光照仍是皮革同一套（凹槽高度场 → 法线 → 左上光源）。

### 坑 / 要留意

- 偏差量级刻意很小（尺子弯曲 <1px），**放大截图才看得出线不是绝对直的**；正常缩放下的感受是"线格更像压在皮里、
  不那么机械"，而不是"明显歪了"。若站长觉得"看不出区别"，加大 `RULER` 各谐波振幅即可，别改成逐点随机。
- 瓦片 560px 在大屏上会周期重复（跟皮革 384px 那张不是整数倍，所以叠起来不会同步重复），肉眼不易察觉。
- 第一版线太软、针孔看不见（比原 CSS 版更弱），调过：凹槽变窄变陡、针孔加大并加亮边。改参数时以"跟原版可读性至少一样"为底线。
- 手机端 `background-size` 同步改成 `560px 560px, 320px 320px`（皮革在手机上是 320）——线格在手机上**不缩放**，
  跟旧版（28px 格子不缩放）行为一致。
- 验证：静态空页面 + 真实 CSS，新旧对比截图（1x、2x、4x 放大）、瓦片接缝处截图；没在真机上看过。

---

## v15 · 2026-09-24 · 任务B第四项：皮革贴片（标签 / 按钮 / 作者徽章 / 标签计数 / 置顶丝带）

站长让我继续、不提意见，这一项同样是我按 `docs/UNFORGEABLE_DESIGN.md` 第6节"直接复用现成系统"那行自己推进的，
没有站长对这一项的反馈。**只改了 `public/css/style.css`，没有新脚本、没有新贴图**——缝线复用 v12 的
`stitch-edge*` 贴图，只是缩到 0.55 倍（线径跟着变细）。

原来这五样分别是描边药丸、纯色矩形按钮、纯色小方块、纯色渐变丝带，都是"给文字套个框"。现在统一成一个物件：
**一小块皮子裁下来，边缘缝一圈线**。

### 规则（写在 style.css 的"皮革贴片"一节，改的时候先读那段注释）

- 贴片用跟所在底色**相反**的皮：米黄卡片上（文章标签、置顶丝带）用靛蓝皮；靛蓝桌面上（评论按钮、作者徽章、
  标签索引的计数）用小牛皮。否则贴片在底上"消失"。
- 粒面大小沿用卡片同一比例（`background-size: 384px`），全站皮的颗粒一样大。
- 文字是"压进皮里"：靛蓝皮上字带一线暗上沿，小牛皮上带一线亮下沿，跟标题 letterpress 同逻辑。
- 缝线的横边挂 `::before`、竖边挂 `::after`，各自在两头让出一个线径——角上是"转弯"，
  不是横竖两条叠成一坨（第一版就是叠成一坨，截图看出来后拆开的）。
- 丝带用 `clip-path` 裁燕尾，`clip-path` 会裁掉外投影，所以丝带没有外投影，只有内高光；只缝两侧竖边。

### 没做 / 要留意

- 评论表单的输入框（`.comment-form input/textarea`）还是纯色矩形，第6节没点名，这次没动，但同样是"假定制"。
- 标签 hover 只是变亮（`filter: brightness`）+ 字变烫金色；按钮按下有 1px 下沉。都是保守做法，站长若要更强的反馈再加。
- 验证：静态测试页 + 真实 CSS + 5 倍放大逐个元素截图核对；没连数据库、没在真机上看过。

---

## v14 · 2026-09-24 · 任务B第三项：烫金分隔线（`docs/UNFORGEABLE_DESIGN.md` 第6节"烫金分隔线"项）

站长确认 v13 及之前已部署，并说"你继续写，我不提意见"——这一项是我自己按第6节的方向选的下一项，
没有等站长挑（所以**没有任何站长对这一项的反馈**，观感是否合意待站长看线上效果）。

三处分隔线原本都是同一条 `linear-gradient(90deg, 铜, 透明)` 的 1~3px 色带（标题栏下沿、页脚上沿、
文章标题下的 72px 短横）。换成 `tools/build-foil.js`（`npm run foil`）生成的两张透明 PNG：
`foil-line-v1.png`（520×10，长线；**v23 起已弃用并删除**，换成 `foil-line-wide-v2.png`）、`foil-bar-v1.png`（96×12，短横）。

### 做法（跟缝线同一条管线，但几何相反）

烫金不是凸起的线，是烫版压出的**凹槽**，箔片转印在槽底：
- 高度场是梯形凹槽（平底 + 两侧斜坡），法线/漫反射/高光复用皮革、缝线同一个光源方向；
- 箔面是**拉丝金属**：沿线方向极度拉长的噪声扰动微法线，加上低频反射带，所以线上不同位置亮度不同；
- **渐隐不是渐变到透明**：是转印覆盖率逐渐下降（漏烫），漏烫处只剩压出来的暗凹槽，末端槽也渐浅到消失；
  这一点取代了原来的"渐变"，也是跟"数学插值"最本质的区别；
- 凹槽中心线沿线有 <0.5px 的极缓慢偏移（对位误差），是整条线连续有记忆的，不是逐点抖动。

CSS 里三处 `::after`/`::before` 换成 `background: url(...) / 100% 100% no-repeat`；长线宽度
`clamp(220px, 26vw, 520px)`（原来是 18% 宽度，现在稍长，因为图里有渐薄的过程要看得到）。

### 坑 / 没做的

- 长线在窄屏是**横向压缩**（`100% 100%`），漏烫斑点会跟着变窄，手机上截图看着正常，但没在真机上看。
- 有效范围：只换了这三处。标签 pill、按钮、书签丝带、评论徽章第6节说"直接复用现成系统"——
  金色材质这一步做完了，这几项**可以开始套了**，但还没做。
- 验证方式：真实 header/footer 模板 + 真实 CSS 静态页截图（桌面 1440、手机 390），没连数据库跑整页。

---

## v13 · 2026-09-24 · 任务B第二项：分页箭头（`rosette()` 派生，`docs/UNFORGEABLE_DESIGN.md` 第6节"★已确认要做"项）

把首页分页里写死的 `&larr;`/`&rarr;` 换成 `src/utils/brand.js` 新增的 `paginationArrow(direction, opts)`：
花饰（`rosette`）当轴心，一根指针从花心指向翻页方向，针尖是实心三角。花饰半径和透明度故意压低一档——
第一版花饰和指针一样粗，两根"胡须"从花心戳出去，看不出方向，调过。

### 改了什么

- `src/utils/brand.js`：新增并导出 `paginationArrow`。
- `src/app.js`：把它挂到 `res.locals.paginationArrow`（EJS 里没法 require，跟 `signatureMark` 同一个做法），默认色烫金 `#C79A45`。
- `views/index.ejs`：分页四处换成内联 SVG；不可点的一侧用 `opacity: 0.32`。
- `public/css/style.css`：`.pagination` 里的链接/禁用项改 `inline-flex` 对齐，hover 时箭头朝指向方向微移 2px。

### 范围与坑

- **只改了前台**。后台（`admin/*`）的 `&larr;`/`&rarr;` 没动——后台约定是纯色 UI、优先好用，不上品牌质感。`views/partials/notebook.ejs` 里的那两个是 v11 之后已经没有路由指向的死代码，也没动。
- 已用真实 CSS + 真实 EJS 片段截图核对过三种状态（首页/中间页/末页），箭头方向、对齐、禁用态都对；没有连真实数据库跑整页。

---

## v12 · 2026-09-24 · 任务B第一项：卡片缝线换成真圆柱截面（`docs/UNFORGEABLE_DESIGN.md` 第6节的"★工程量大"项）

站长直接给了写好的 `tools/build-stitch.js`（这轮不是我从零设计的，是站长按文档第4节"皮革靠结构复杂度"
的判断依据自己写的方案，我负责验证、修 bug、接进真实页面）。做法见脚本头部注释：不再用
`repeating-linear-gradient` 画"一段颜色一段透明"的平面矩形，而是复用皮革同一套高度场→法线→
漫反射+高光管线，把线当成"穿过皮革的圆柱体截面"来渲染，针脚段之间有真实的针孔凹痕，线的粗细/
张力沿整条线连续起伏（不是逐点白噪声）。

### 改了什么

- 新增 `tools/build-stitch.js`、`npm run stitch`，产物 `public/img/stitch-edge-v1.png` /
  `stitch-edge-vert-v1.png` / `stitch-edgeAlt-v1.png` / `stitch-edgeAlt-vert-v1.png`
  （横版 54×22，竖版 22×54，透明底，横向无缝可重复）。
- `public/css/style.css` 的 `.post-card::before, .post-article::before`：四条
  `repeating-linear-gradient` 换成四层 `background-image`（top/bottom 用 `stitch-edge`，
  left/right 用 `stitch-edgeAlt` 的竖版——两条边节奏刻意不同，不是同一张图四个方向复制）。
  去掉了原来的 `filter: drop-shadow`，新贴图自己已经算好了线压进皮革的投影。移动端
  `inset:7px` 那条改成对应的 `background-position` 偏移。

### 坑

- **`sharp` 的一个真实 bug**：脚本原来在同一条"raw 像素输入 → `.resize()` → `.clone().rotate(90)`"
  的管线上直接生成竖版贴图，结果旋转会退化成 22×22 的正方形，而不是该有的 22×54——**同样的
  `.rotate(90)`，如果先把图落地成真正的 PNG buffer、再从这个 buffer 建一个新的 `sharp` 实例去转，
  就是对的**。以后碰到"resize 之后接 rotate 结果不对"，先怀疑是不是喂的是 raw buffer 没先落地。
  已经在 `build-stitch.js` 里修好（`baseBuf` 那段）。
- **视觉验证走的是真实浏览器，不是凭代码想象**：起了一个本地静态服务器 + Playwright，把真实的
  `.post-card` 结构（含置顶书签丝带、正文、`post-meta`）套上真实 `style.css` 截图看过桌面
  （900px）和移动端（375px，触发 `@media (max-width:640px)`）两种宽度，确认缝线在真皮革底纹上
  的观感、置顶丝带右上角不会被边线穿过、转角处线头交叠的样子。截图没有存进仓库，只是本地验证。

### 还差什么

`docs/UNFORGEABLE_DESIGN.md` 第6节表格里"原创材质"这一类下，缝线只是其中一项，**网格线+针孔、
烫金分隔线、标签pill/按钮/书签丝带/评论徽章这几项还没动**（后两者说明里写的是"复用现成系统"，
理论上应该比缝线轻）；"原创图形"类的引用装饰引号、分页箭头（`rosette()` 派生）也都还没做。
下一步接着按第6节的表格顺序推进。

---

## v11 · 2026-09-23 · 放弃两页对开+翻页动效，改成单页"日记目录"

### 起因

v10 部署后站长反馈两点：①还是不像一本书，是"两个很奇怪的页面"；②真正的初衷是
"日记本形式"没错，但**是 AI（我）把它做得太复杂了**，要求大改。又追加以美术师、
产品销售经理两个角色做了一轮纯专业视角的分析（不看文档），指出的核心问题：
跨页对开这个结构本身就是复杂度的根源（为了"像书"才需要装订缝/跨页对齐/3D
透视/书脊标签磨损这些工程量）；材质和装饰跟网站其余地方（密码学级防伪水印、
离线生成的皮革纹理）投入的"手艺"不对等，用的是随处可见的"少女心手帐"装饰
套路；交互上强制翻页动效（哪怕内容早就 fetch 完）、全文用弹窗展示不给真实
URL，都是会拖慢用户看到内容的机制性问题。

站长最终拍板：**不做成一本能摊开的书，做成"书目录"——单页展示，标题直接是
真实链接，点了就跳文章页，不要翻页动效**。

### 改了什么（结构性重写，不是修修补补）

- **`views/partials/notebook.ejs`**：整个重写。从"封面+跨页+贴纸抽屉+翻页舞台"
  精简成一张目录卡片：标题「目录」+ 当前季度标签、搜索框、季度筛选下拉、标签
  列表（真实 `<a href="/tag/:slug">`）、目录条目（标题…虚线引导线…日期，经典
  书籍目录排版）、分页。**条目标题是真实的 `<a href="/p/:slug">`**，不再有
  "点击→动效→弹窗"这一层。
- **`public/css/notebook.css`**：整个重写，**直接复用 `.post-card` 已经调好的
  皮革/纸张材质**（`public/css/style.css`），不再单独定义一套纸张贴图或紫外线
  做旧效果——这样首页和文章卡片才是真的"同一个品牌"，而不是material分裂。
  去掉了：装订缝渐变、跨页透视、书脊标签磨损插值、贴纸/拍立得/大头针装饰、
  Caveat 手写字体。
- **`public/js/notebook.js`**：整个重写，从 392 行砍到不到一半。去掉了
  `playFlip`/`runFlying`/`backToToc`/`pingView` 这一整套翻页动效编排、书脊标签
  磨损计算、贴图 hash 分配。剩下的逻辑就是：拉季度列表 → 拉目录数据 → 渲染
  成列表 → 季度筛选/搜索时重新拉数据重新渲染，标题不需要任何点击处理逻辑，
  原生 `<a>` 链接自己会跳转。
- **`src/routes/public.js`**：
  - `/api/notebook/toc` 不再返回 `pageTexture`/`decoration`/`tapeAngle`，`perPage`
    从 6（"一页书放4-6篇"）改成 10（普通目录不用受física页面大小限制）。
  - **删掉** `/api/notebook/article/:slug`（原来给弹窗读全文用的文章片段接口）
    和 `/api/notebook/article/:slug/view`（片段阅读的静默计数）——直接跳
    `/p/:slug` 后，这两个接口的活儿被现成的 `/p/:slug` 路由自己的阅读计数覆盖了，
    不用重复造一套。
  - **关掉** `/dev/notebook` 静态预览路由——对应的两页对开原型
    （`experiments/notebook-spread/`）已经是放弃的设计方向，不再需要在线上
    留个入口能访问到它。原型文件本身还在仓库里，只是没有路由指向了。
- **`src/utils/notebook.js`**：整个删除——`pickPageTexture`/`pickImageDecoration`/
  `pickTapeAngle` 这几个"按内容 hash 确定性分配材质/装饰"的函数，新设计压根
  不需要材质分配了。
- **`views/partials/head.ejs`**：去掉了 Caveat 手写字体的 `<link>`，日期/标题
  现在复用全站正文/标题字体，不再引入第三套"笔迹"。
- **`views/index.ejs`**：`<main>` 不再需要 `has-notebook` 这个特殊宽度类
  （跨页结构没了，目录卡片跟其他卡片一样用全站标准内容宽度 `--max-w: 42rem`）。

### 测试方式（老实说清楚，别夸大）

沙箱环境没有数据库、也没装项目全部依赖，没法把整个 app 跑起来做端到端验证。
**这次单独装了 `ejs` 这一个包**，脱离 app 直接对 `views/index.ejs` 和
`views/partials/notebook.ejs` 做了离线渲染（手动 mock 了 `posts`/`siteName`
等 locals），确认：
- 两份模板改完之后 EJS 语法是对的、能渲染出完整合法的 HTML
- `#plain-post-list`、`#notebook-book` 两个容器都在，结构符合预期
- **这个过程中真的抓到一个 bug**：我重写 `notebook.ejs` 时漏掉了文件末尾那行
  加载 `public/js/notebook.js` 的 `<script>` 标签——这行之前只存在于这一个
  文件里，别处没有引用它，我如果没做这次离线渲染测试，直接推上去的话目录
  视觉层会完全不出现（`#notebook-book` 永远是 `hidden`，因为没有 JS 去把它
  显示出来），退化成一直显示朴素卡片列表，而且不会报错、不容易第一时间发现。
  **已经补回去，补完之后重新渲染确认过。**
- `public/js/notebook.js`、`src/routes/public.js` 过了 `node --check` 语法检查。
- **没有测的**：没有连数据库跑真实的 `/api/notebook/*` 接口，没有在浏览器里
  实际点过搜索/筛选/分页交互，没有验证过响应式在真实桌面宽度下的排版观感。
  这些站长部署后最好实际点一遍，尤其是搜索输入、季度切换这两个新交互。

### 部署

```bash
cd /var/www/blog-blue && sudo -u blogblue git pull && sudo -u blogblue pm2 restart blog-blue
```

删了一个文件（`src/utils/notebook.js`）、改了几个路由和模板，没有数据库迁移、
没有新的 npm 依赖，`git pull` + 重启进程就够。

---

## v10 · 2026-09-23 · 纸张材质统一 + 封面/目录页宽度对齐

### 起因

站长看完 v9 部署效果后反馈两点：①还是不像一本书，是"两个很奇怪的页面"；
②纸张材质"一开始就错了"——太追求旧感，要求保留封面的颗粒皮革，但纸张改成
统一的米黄色淡线条纸。并要求接下来的分析不要看文档，直接以设计师视角评估
成品可用性——这轮之后的可用性分析没有写进本文件，站长自己看聊天记录。

### 改了什么

- `public/css/notebook.css`：`.nb-cover` 宽度从 260px 改成 340px，和
  `.nb-toc-page` 完全一致（之前两页宽度不一样，即使高度对齐了，视觉上还是像
  两张不同大小的卡片拼在一起）。`.nb-emblem` 靠 `left:50%` 居中，`.nb-ribbon`
  是靠左边距定位，两者都不依赖具体宽度数值，改宽度没有连带牵动内部元素摆位。
- `.nb-toc-page`、`.nb-flying-page`：背景从按文章 hash 分配的 P0~P4"做旧"贴图
  （茶渍/水渍/晒痕/包浆）改成统一的 `var(--paper)` 米黄色 + 淡横线条
  （`repeating-linear-gradient`），翻页动效途中也不再闪过深浅不一的贴图。
- `public/js/notebook.js`：对应去掉了两处往 DOM 上注入 `page-P*-v1.webp` 贴图
  URL 的代码。**后端 `pickPageTexture` 和 `/api/notebook/toc` 返回的
  `pageTexture` 字段没有动**——前端只是不再消费它，接口形状没变，以后如果要
  恢复分级材质也不用改后端。

### 没做的

同 v9，两页的透视/摊开立体感（`rotateY` 静态基准）还是没做，原因不变
（会被 `notebook.js` 里翻页/切换分册的动效绝对赋值覆盖，需要一起改）。

### 部署

```bash
cd /var/www/blog-blue && sudo -u blogblue git pull && sudo -u blogblue pm2 restart blog-blue
```

只改了 CSS/JS，不需要 `npm install` / `npm run migrate`。

---

## v9 · 2026-09-23 · 修"这不像一本书"的三处几何/光影错误

### 起因

站长部署 v8 之后反馈：问题不是"手帐调性太业余"，是**这根本不像一本书**。对照
`experiments/notebook-spread/README.md` 里"装订缝处理…未锁定状态，不代表定稿"这句，
去代码里核对了一遍，找到三处不是审美问题、是**几何/物理逻辑本身就没对**的错误：

1. **左右两页高度对不上**：`.nb-cover` 写死 `height: 354px`，`.nb-toc-page` 是
   `min-height: 408px`，相差 54px。真实书摊开后左右两页必须顶部/底部对齐（装订在
   一起），高度不一致会被读成"两张大小不同的卡片"，是目前最大的破绽。
2. **装订缝阴影方向反了**：`.nb-gutter` 原来是"两端暗、中间亮"，跟真实书脊凹陷
   的光影正好相反——凹槽最深处（正中间）应该最暗，越靠近两侧书页表面越亮。原来的
   效果看起来像"中间插了一根独立分隔杆"，不是"书页从这凹下去"。
3. **两页边缘跟装订缝之间没有阴影过渡**：封面右边缘、目录页左边缘原来都是平的，
   没有暗示"正在往装订缝方向弯进去"，跟中间的沟槽完全脱节。

### 改了什么（`public/css/notebook.css`，都是纯几何/渐变修正，不涉及审美取向）

- `.nb-cover` 去掉写死的 `height: 354px`——`.nb-spread` 是 `display:flex` 且没设
  `align-items`（默认值就是 `stretch`），去掉固定高度后封面会**自动拉伸到跟
  `.nb-toc-page` 一样高**，以后目录页因为内容变化改高度，封面也会跟着变，不会再错开。
- `.nb-gutter` 渐变方向反过来：中间最暗（`rgba(0,0,0,0.52)`）、两侧渐亮
  （`rgba(0,0,0,0.08)`），读起来是凹槽而不是分隔条。
- `.nb-cover` 右边缘、`.nb-toc-page` 左边缘各加一条 `inset box-shadow`，往装订缝
  方向压暗，跟 `.nb-gutter` 的中心暗带接成一条连续折痕。

### 没动的（有风险/需要站长定调，没有先斩后奏）

- **两页宽度不一致**（封面 260px vs 目录页 340px）：这个我判断不了要不要改——
  改成一样宽会牵动封面里已经调过的签名/书签带定位（`.nb-emblem`、`.nb-ribbon` 都是
  按 260px 宽度调的百分比/像素混合定位），不是改一个数字就完，需要重新摆位置，
  风险比这次三处纯几何修正高得多，而且我这边没法截图确认摆得好不好看。如果站长
  觉得这处宽度差异也破坏"这是一本书"的观感，告诉我一声，我按这个方向重新调。
- **摊开角度的立体感**（两页各带一点透视旋转，往装订缝方向收拢）：技术上可以用
  `perspective` + `rotateY` 做，但 `public/js/notebook.js` 里翻页/切换分册动效已经
  在用 `tocPageEl.style.transform = 'rotateY(...)'` 做绝对赋值（第173-185行），
  如果我再在 CSS 里给静态摆放加一个基准 `rotateY`，会被动效每次结束时重置成
  `rotateY(0deg)`，翻页互动几次之后立体感就会消失——这个要做得先跟 JS 那部分动效
  逻辑一起改，不是这次这种纯 CSS 改动能顺手带上的，先没动。
- 本轮同样没有截图验证（沙箱环境限制，历次记录一致），三处都是可推导的几何/渐变
  逻辑修正，风险相对低，但部署后还是需要站长实际看一眼两页顶部/底部是否对齐、
  中间折痕是否读得出来。

### 部署

```bash
cd /var/www/blog-blue && sudo -u blogblue git pull && sudo -u blogblue pm2 restart blog-blue
```

只改了一个 CSS 文件，不需要 `npm install` / `npm run migrate`。

---

## v8 · 2026-09-23 · 翻书首页上线后视觉走查（对应 v7 遗留的"步骤9 整体走查"）

### 起因

v7 上线后没有截图/视觉反馈闭环（沙箱装不了 headless Chromium），站长发来真实首页截图，
以专业角度指出几处问题。这一轮**只处理了截图里能定位到根因、且改了不会牵连设计决策的
纯 CSS 问题**，不是完整的第9节走查。

### 改了什么（`public/css/notebook.css`）

1. **日期/分册号对比度不够，糊成一片**：`.nb-vol`、`.nb-entry-date` 之前直接用
   `--brass-dim`（`#9C7A38`）压在纸面底纹（`page-P*-v1.webp`，平均亮度偏高）上，
   实际对比度不到 2:1，肉眼几乎看不清（截图里"9.22""9.18"这类日期就是这个问题）。
   新增一个专用变量 `--nb-ink-brass`（`#5C3F14`，深钢笔墨水色），只用在纸面手写批注上，
   **没有改 `--brass-dim` 本身**——它还在标签 hover 等别的地方用，改了会牵连别处。
   顺带去掉了额外叠加的 `opacity: 0.8/0.9`（对比度已经跟原来不是一个量级，不需要再叠减）。
2. **标签/搜索便签像悬浮的独立卡片，跟页面没关系**：`.nb-note-tags`/`.nb-note-search`
   原来的偏移量（`top:-14px`、`left:-16px`）让便签条大半悬在纸面外，配合截图里没有阴影
   过渡，读起来是"飘在旁边"而不是第5节说的"夹在目录页外围、露出一角"。收窄了悬出量，
   让便签大部分压在纸面上，只留小半个角在外；加了一个 `::before` 做的模糊阴影条，
   让它视觉上像是真的搭在纸面边缘、被压住了一角，而不是纯粹叠在最上层。

### 没动的（截图里提到但这轮没处理）

- **测试文章数据**（"h in"、"123"）：这是数据库内容，不是代码问题，我这边没有生产库
  访问权限。**需要站长自己登录后台删掉**，删完这两条之后目录页的排版会更接近真实效果——
  现在因为这两条没摘要，标题和正文的字号/字重层级看起来比实际情况更混乱。
- **大屏两侧留白大**：这是"书摊在桌上"这个设计语言本身带出来的效果（书本身宽度固定，
  没有拉伸铺满整个视口），改动会牵扯到第0-1节定的设定，**没有先斩后奏去改**——如果站长
  觉得留白太空，需要先确认是要保留"桌面留白"这个概念（比如往桌面上加点其他装饰物垫一下）
  还是要放弃固定宽度、让书本身跟着视口变宽，这是设计方向的决定，不是能直接"修"的 bug。
- 便签造型的随机分配（第5节说要"方形便签/索引卡/书签丝带/回形针纸条"四种池子）、
  移动端真机验证——这些是 v7 遗留清单里本来就没做的，跟这次截图问题无关，没有顺手做。
- 没有截图验证改完之后的实际观感（沙箱环境依旧没有 headless Chromium，见 v3/v7 同类记录）。
  **这轮改动只是 CSS 变量替换和数值调整，风险低，但站长部署后还是应该看一眼实际效果。**

### 部署

```bash
cd /var/www/blog-blue && sudo -u blogblue git pull && sudo -u blogblue pm2 restart blog-blue
```

只改了一个 CSS 文件，没有新依赖、没有数据库迁移，不需要 `npm install` / `npm run migrate`。

---

## v7 · 2026-09-23 · 翻书改版：步骤6/7 + 正式接入真实首页

### 发生了什么

`experiments/notebook-spread/` 里几轮原型（书脊分册标签、跨页目录、点击进入全文的翻页动效）
一直只挂在需要登录才能看的 `/dev/notebook/*` 预览路由下，首页 `/` 本身没有变化。这一轮把它
**正式接进真实首页**，同时补上了执行计划里还没做的步骤6（标签/搜索便签系统）和步骤7（图片
装饰系统接入真实上传图片）。

### 改了哪些文件

- **新增**：`src/utils/notebook.js`（确定性 hash 分配：内页材质/图片装饰方式/胶带角度）、
  `public/js/notebook.js`（首页翻书 UI 的全部前端逻辑）、`views/partials/notebook.ejs`
  （翻书 DOM 结构）、`public/css/notebook.css`（翻书视觉样式）、
  `tools/build-notebook-textures.js`（内页材质生成脚本，移进仓库，算法未改）、
  `public/img/notebook/page-P0-clean-v1.webp` ~ `page-P4-handling-patina-v1.webp`（生成产物）。
- **修改**：
  - `src/models/tag.js` 新增 `listAllPublic`（公开标签云：只统计已发布文章、过滤 0 篇的空标签）。
  - `src/models/post.js` 新增 `searchPublished`（标题/摘要 `ILIKE` 子串匹配，`limit` 固定 8）。
  - `src/routes/public.js`：首页路由 `/` 现在会算 `notebookEnabled`（仅首页第1页为真）和
    `notebook`（品牌签名 SVG，深/浅两版）传给模板；新增 `GET /api/notebook/tags`、
    `GET /api/notebook/search`；已有的 `/api/notebook/volumes`、`/toc`、`/article/:slug`、
    `/article/:slug/view` 未改动。
  - `views/index.ejs`：`#plain-post-list` 包住原有服务端渲染列表（无 JS/爬虫/移动端兜底），
    `notebookEnabled` 为真时额外 `include('partials/notebook')`。
  - `views/partials/head.ejs`：`notebookEnabled` 为真时才加载 Caveat 字体和 `notebook.css`
    （不拖慢文章页/标签页）。
  - `public/css/style.css`：补了 `.empty-hint` 这个小遗漏（之前"没有文章"的提示文字是行内
    `style="..."`，在生产 CSP 下不生效，现在用 class）。
  - `package.json`：新增 `notebook:textures` 脚本。

### 验证过什么（真实数据库，不是只读代码）

沙箱内装了一次性 PostgreSQL 16，跑了 `npm run migrate`、`npm run seed`，又手工插入 15 篇跨两个
季度、带标签和封面图的测试文章，起了真实 `src/app.js`，用 curl 验证：

- `notebookEnabled` 只在首页第1页为真：`/` 有 `#notebook-book`，`/?page=2` 和 `/tag/life` 都没有。
- 六个 `/api/notebook/*` 接口全部返回正确结构：`volumes`（按年季度分组）、`toc`（含
  `pageTexture` 和每条目录的 `decoration`/`tapeAngle`）、`tags`、`search`、`article/:slug`、
  `article/:slug/view`；边界情况对：`quarter=9` 返回 400，越界分页返回空 `entries` 而非报错，
  不存在的 slug 返回 404。
- `POST /api/notebook/article/:slug/view` 真的让 `posts.view_count` +1。
- `/dev/notebook/*` 未登录访问返回 302 跳 `/admin/login`（`requireAuth` 沿用不变）。
- 静态资源都能正常拿到：`/css/notebook.css`、`/js/notebook.js`、
  `/img/notebook/page-P0-clean-v1.webp`。
- 回归测了没被这轮改动波及的路由：`/feed.xml`、`/sitemap`、`/sitemap.xml`、`/p/:slug`、
  `/robots.txt`、404 页面，全部 200/预期状态码。

### 没做 / 没验证的（老实说，比 v3/v4 那两轮验证得浅）

- **一张视觉截图都没有**：沙箱依旧装不了 Playwright 的 headless Chromium（`experiments/
  notebook-spread/README.md` 好几轮都提过同一个环境限制），这轮全部验证停在"接口数据对、
  DOM 结构对"，翻书本体的实际观感——纸张痕迹材质是否可辨识、胶带/拍立得/大头针装饰是否可信、
  标签便签"掀开一角"的动效顺不顺——一次都没有被看过。**这是部署后最需要站长亲自确认的部分**。
- 步骤6的便签样式池按第5节规格应该是"方形便签、卡片式索引卡、书签丝带、回形针夹的小纸条"
  四种随机分配，这轮只做了固定搭配（标签配索引卡、搜索配回形针纸条），没做另外两种样式、
  也没做随机分配——两个便签谈"随机分配"意义有限，先按最省事的方式接了，如果站长想要更多
  样式差异化需要重做。
- 步骤9（整体走查，对照第2节禁忌清单逐条检查）完全没动，是下一轮必须做的收尾。
- 移动端隔离靠已有的 CSS 媒体查询（`max-width:640px` 强制 `display:none`）验证了逻辑存在，
  但没有真机/多浏览器测试过实际断点手感。

### 部署

```bash
cd /var/www/blog-blue && sudo -u blogblue git pull && sudo -u blogblue npm install && sudo -u blogblue npm run migrate && sudo -u blogblue pm2 restart blog-blue
```

没有新的数据库迁移（这轮没碰 schema），标准部署命令就够。**新增了一个环境变量无关的一次性
资源生成步骤**——`public/img/notebook/*.webp` 这五张图已经跟着这次提交一起进了仓库（和
`leather-*-v1.webp` 同一套约定），`git pull` 之后不需要在服务器上再跑
`npm run notebook:textures`，除非以后要改材质配方换版本号。

### 给接手的人

- 这次是在 `docs/BOOK_DESIGN.md` 第11节和上面"没做/没验证的"里已经写清楚的收尾工作，
  不要重复分析一遍，直接从那两处的清单接着做。
- **第一件事应该是站长打开首页实际看一遍**——这轮完全没有视觉反馈闭环，接手者不要在没有
  站长审美确认之前就假设步骤6/7的具体样式（便签造型、装饰细节）是定的。

---

## v6 · 2026-09-23 · 翻书改版重启——新方向：封面复用现有材质（进行中，只做到封面）

### 发生了什么

站长在 v5 否决翻书改版之后，又主动重新提起，但换了方向：上传了 4 张市售笔记本参考图
（烫金压印/皱纹皮/搭扣设计/星月图腾）+ 1 张线上截图，要求封面**直接复用已经上线的贴图**
（`public/img/leather-calf-v1.webp`，`.post-card` 正文区在用的那张），不再单独生成新材质；
参考图的"中心图腾+装饰边框+五金件"结构可以借，但具体图案要换成 blog.blue 自己的语言。

### 处理方式与已确认的决策

一步步来、每步先分析再动手（按协作约定），确认了这些点（详细对照表见
`experiments/notebook-cover/README.md`）：

- 封面材质 = 线上现成的 `leather-calf-v1.webp` / `leather-cream-v1.webp`，粗粒羽化边、缝线
  针脚直接复用 `.post-card` 现成 CSS，没有新画一套。
- 中心图腾 = 放大版品牌签名词标本身（不是参考图里的通用羽毛/月亮），**卸刻凹陷感**（深色主线
  + 偏移 1.6px 的浅色高光副本，不是烫金，没用滤镜）——这是站长明确选的。
- 装饰边框 = 罗盘弧线 + 四角十字校准记号 + 刻度短线，代替参考图的巴洛克唐草——那套花纹和
  `README.md` 明确要避开的"暖米色+套路风"是同一类风险。
- 加了一根烫金书签带，纯装饰、不可点击，站长确认要加。

原型脚本和文件：`experiments/notebook-cover/build-cover-prototype.js`（跑一下会生成
`cover-prototype.html`，读的是线上正在用的贴图和真实品牌签名，不是写死的假数据）。

### 给接手的人

- **这个任务目前是进行中，只做到"封面"这一步**，还没往下接内页排版——继续做之前，先读
  `experiments/notebook-cover/README.md` 里"还没做的/下一步"。
- **不要去翻 `experiments/book-materials/` 接着做**——那是 v5 里被否决的深棕牛皮革方向，
  和这次的方向不是一回事，纯历史参考。
- 封面已经定的这几条设计决策（材质来源、图腾质感、边框风格、书签带）都是站长明确确认过的，
  不要因为"觉得能做得更好"就自己改掉；真要改，先出新原型给站长看，别直接改完再展示。

---

## v5 · 2026-09-22 · 翻书改版取消（非代码改动，任务终止）

### 发生了什么

`docs/BOOK_DESIGN.md` 步骤3（把已定稿的封面/内页材质套进完整书本排版）出了一版静态原型给站长看
（原型是一次性生成的独立 HTML，没有进正式代码、没有留在仓库里）。站长看完后的反馈是：**不是原型某处
细节需要调，是整体上不想再推进"翻书改版"这个视觉方向了**。

### 处理方式

- `docs/BOOK_DESIGN.md` 顶部加了取消说明；文档本身保留（材质定稿过程中的踩坑记录仍有参考价值）。
- `experiments/book-materials/` 材质脚本原样保留，不删除。
- 首页维持现在的卡片列表实现（`views/index.ejs` + `public/css/style.css` 的 `.post-card`）不动。
- 没有任何正式代码改动、没有新迁移、**不需要部署**。

### 给接手的人

**这个任务已经结束**，看到 `docs/BOOK_DESIGN.md` 或 `experiments/book-materials/` 不要默认还在进行、
自动往下做步骤4及之后。如果站长以后又提起"首页想换个视觉"之类的话，**当成新需求重新确认**，
不要直接把这份旧方案捡回来接着做——他否决的是方向本身，不代表下次想法还一样。

---

## v4 · 2026-09-20 · 评论审核重做（任务二，和 v3/v3.1 的视觉任务分开）

### 诉求

站长：以专业设计师的身份重新设计后台评论审核，建议把**文章和评论绑定**以便后续操作，具体由我分析和设计。

### 旧版的问题（分析）

1. **只有"待审核"一个视图**：通过 / 标垃圾之后评论就从界面上消失了。误点垃圾 = 永远看不到；没法回看已通过的；没法删除。
2. **评论和文章是脱节的**：一条评论只有一个文章标题链接。文章列表里看不出哪篇有待审评论；进评论页看不到"这篇文章一共有几条、说了什么"。
3. **每次操作整页刷新**回到顶部，处理 20 条 = 20 次刷新。
4. **作者不能回复**：表里有 `parent_id`，但从来没用过，评论是单向的。
5. 没有批量、没有键盘操作、看不出"是不是同一个人在刷"。
6. 评论功能默认是**关闭**的，评论页却完全不提示。
7. 侧栏没有待审数字，只有仪表盘顶部一条 banner。

### 设计思路

把它当成**邮件收件箱**来做（每天要重复做的分拣工作），并且**以文章为单位组织**：

- **信息架构**：`/admin/comments` 是全站收件箱（默认待审，先来先处理）；`/admin/comments?post=<id>` 是**文章绑定视图**——顶部是文章卡片（状态、三种数量、编辑文章/在站点查看），评论按**对话顺序**排列，回复缩进一层跟在上级下面。
- **左侧"按文章"栏**：只列有评论的文章，待审多的在前，带数字。
- **状态标签页**：待审核 / 已通过 / 垃圾 / 全部，数字跟着当前范围走（选了文章就是这篇的数字）。垃圾是**可恢复的暂存区**，永久删除只出现在垃圾箱和作者自己的回复上，并二次确认。
- **文章 ↔ 评论双向互链**：文章列表新增"评论"列（待审 pill + 已通过数，点了直接进该文章的评论）；文章编辑页右侧有"本文评论"入口；侧栏"评论"上有待审角标；评论卡片上有文章链接，已通过的评论有"在文章中查看"（跳到前台 `#c-<id>` 锚点并高亮）。
- **不刷新 + 可撤销**：所有动作是 fetch + JSON，服务端返回**重新渲染好的卡片 HTML**，前端只做替换（卡片长什么样只有 `views/partials/comment-card.ejs` 一处说了算）；通过/垃圾/退回后 7 秒内可撤销（按 `U` 也行），撤销会按原位置插回去。
- **键盘**：`J/K` 上下、`A` 通过、`S` 垃圾、`R` 回复、`X` 选择、`Ctrl/⌘+Enter` 发送回复。中文输入法组词时不拦截按键。
- **作者回复**：直接通过、标"作者"、前台也显示徽标并缩进；**回复一条待审评论 = 认可它**，上级会一并通过（一步完成"通过 + 回应"）。
- **访客识别**：卡片显示 `访客 #a3f9c1 · 共 N 条`（按 IP 哈希，不存明文）；同一访客有多条待审时出现"该访客的 N 条待审全部标垃圾"（已通过的不动）。
- **批量**：勾选后浮出操作条（通过/退回/垃圾/删除），"全选本页"。
- 评论关闭时页面顶部有提示和设置入口。

### 改了哪些文件

- `db/schema.sql`：`comments.is_author` 列 + 两个索引（可重复执行）。`src/models/comment.js` 的 `ensureSchema()` 在**应用启动时**也会跑同样的语句，所以即使忘了 `npm run migrate` 也不会 500。
- `src/models/comment.js`：重写（列表/统计/文章栏/线程排序/批量/回复/同访客）。
- `src/routes/admin.js`：`/comments`（GET）、`/comments/:id/status|delete|reply|spam-visitor`、`/comments/bulk`；侧栏角标中间件；仪表盘和编辑页带评论数。旧的 `/comments/:id/approve|spam` 已删除。
- `views/admin/comments.ejs`、`views/partials/comment-card.ejs`、`public/js/admin-comments.js`、`public/css/admin.css`（收件箱样式）。
- 前台 `views/post.ejs`、`src/routes/public.js`、`public/css/style.css`：评论按对话排序、作者徽标、`id="c-<id>"` 锚点。

### 顺手修的两个真问题

- **前台提交评论的 `parent_id` 没校验**：乱写一个值会让 Postgres 抛类型错误（500），指向别的文章的评论会把评论挂错地方。现在必须是合法 UUID 且是**同一篇文章里已通过**的评论，否则当作顶级评论。
- **CSP 会拦掉 `style="..."` 行内样式**（`styleSrc` 只有 self + nonce）。编辑页那个 `style="display:none"` 的文件输入在生产环境其实是显示出来的；已改成 `hidden` 属性。**其他视图里零星的行内 style（间距之类）同样不生效**，只是不影响功能，没有动。新代码里不要再写行内 style。

### 验证

这次装了真的 PostgreSQL 16，用**真实应用 + 真实 session/CSRF** 跑了：模型 25 项（含线程、级联删除、跨文章 parent 拒绝、批量、非法 id 安全）+ Playwright 端到端 40+ 项（登录、通过/撤销、键盘、同访客批量垃圾、垃圾箱恢复/永久删除、文章绑定视图、回复通过上级并嵌套、批量条、编辑页互链、前台线程/徽标/锚点、XSS 内容被转义）。全部通过。同时也**确认了 v3 的 `/admin/preview` 在真实 CSRF 链路下正常**。

### 部署

```bash
cd /var/www/blog-blue
sudo -u blogblue git pull && sudo -u blogblue npm install
sudo -u blogblue npm run migrate     # 加 is_author 列；忘了也不会出错，启动时会自动补
sudo -u blogblue pm2 restart blog-blue
```

### 没做 / 下一步

- **读者回复读者**：现在前台没有"回复"按钮，线程目前只由作者回复产生。要做的话前台加"回复"并带 `parent_id`（服务端校验已经在了）。
- **新评论邮件/Webhook 提醒**：现在只有后台角标，不主动通知。
- **按文章关闭评论**：需要给 `posts` 加列。
- **垃圾评论自动规则**（链接数量、重复内容）：目前只有蜜罐 + 人工。
- 评论搜索。

---

## v3.1 · 2026-09-20 · 卡片质感修正：图与底分层

### 起因

站长看了 v3 后的反馈：靛蓝皮革桌面很舒服，但**文章卡片也是同一种粗粒皮革，读久了视觉疲劳，抓不住主体**。

### 判断

问题不在皮革本身，而在**整个画面只有一种质感，没有层级**：桌面（忙）和卡片（同样忙）在争夺注意力，正文压在最忙的那层上。
排版上的原则是"越靠近要读的东西越安静"，所以把质感分层：

桌面（有纹理、暗）→ 卡片边缘（粗粒、亮）→ 卡片中心（细腻小牛皮，几乎是平的、最亮）→ 文字

### 做了什么

- 新增一张 `leather-calf-v1.webp`（`tools/build-leather.js` 的 `calf` 配方）：粒极细（6px）、无褶皱、无毛孔、反差约 ±2–3%，离远看几乎是平的，凑近才觉得是皮。卡片正文区用它。
- 原来的粗粒 `leather-cream-v1.webp` **只留在卡片最外一圈**（文章 40px、列表卡片 30px、手机 20px/18px），向中心羽化消失；缝线正好落在这一圈里。实现是 `::after` 上一层粗粒背景 + 两条垂直渐变遮罩取并集（= 一圈羽化边框），`z-index:-1` 垫在正文下面。
- 卡片纸色略提亮（`#F6EFE0`），拉开和桌面的明度差，主体更突出。
- 后台预览面板同步换成 calf，保持"预览 = 发布后的样子"。
- 桌面（靛蓝皮 + 压痕线格）**没动**，站长明确说舒服。

### 坑

- 手机上卡片内边距只有 24px，粗粒边必须比它窄，否则会蹭到正文；已在媒体查询里收窄。
- `--rim` 不要大于卡片的 padding。改 padding 时记得一起看这个变量。
- 想调整"质感强度"，改 `calf` 配方的 `lightGain` / `strength`，然后 `npm run leather`，**并把文件名升到 `-v2`**（静态资源缓存 7 天）。

---

## v3 · 2026-09-20 · 写作系统升级 + 皮革质感视觉

### 背景（founder 的原始诉求）

1. 写文章时要用到 `#`、`**`、`> ` 这类符号，希望编辑页有一张"使用表"
2. 以产品设计师 + 写作者的视角做便捷辅助写作工具；以专业开发者的视角把写作系统整体升级
3. 以高端奢侈品设计师的视角，把文章展示页背景改成**皮革粗糙高端质感**，围绕 blog.blue 做独一无二的风格；蓝色线格背景同样升级

### 做了什么

**A. 写作系统（`views/admin/editor.ejs` + `public/js/admin.js` + `views/partials/md-cheatsheet.ejs`）**

- **工具栏**：H2/H3/H4、粗体/斜体/删除线/高亮/行内码、列表/编号/待办/引用/提示框、链接/图片/代码块/表格/分隔线。再点一次是取消，不是叠加。
- **使用表**（Ctrl+/）：右侧抽屉，"你写什么 → 得到什么"，**每一行可点击直接插入**，和工具栏走同一张 `COMMANDS` 表，行为不会不一致。另有"大纲"（按标题跳转）和"快捷键"两页。
- **实时预览**（写作/分栏/预览三种视图）：走服务端 `POST /admin/preview`，用的就是保存时的 `renderMarkdown`，所以预览 = 发布后的样子，不存在浏览器里另写一套渲染而走样。预览面板用的是和前台同一套皮革版式。
- **智能编辑**：列表/待办/引用里回车自动续行，空项回车结束；Tab/Shift+Tab 缩进列表（按父级标记宽度对齐：`- ` 缩 2，`1. ` 缩 3）；选中文字后粘贴网址自动变链接；**粘贴/拖入图片直接上传并插在光标处**（先放占位符，上传期间继续打字也不会插歪）。
- **安全网**：正文每 1.5 秒暂存到本机 localStorage，意外关页后再打开提示恢复；有未保存改动时离开页面会提醒；保存成功（`?saved=1`）后自动清掉暂存。
- 其他：字数/阅读时长/选中字数、专注模式（Ctrl+Alt+F，Esc 退出）、"从正文生成摘要"。

**B. Markdown 渲染新增语法（`src/utils/markdown.js`）**

- `==高亮==` → `<mark>`（前台是金色荧光笔下划）
- `> [!NOTE|TIP|WARN|IMPORTANT]` → 提示框（语法和 GitHub alert 一致，别处写的文字搬过来不用改）
- h2/h3 自动带锚点 id（中文标题保留汉字，重名自动加序号）
- 表格、任务清单、四级标题在前台补了样式（以前能渲染但没样式）

**C. 皮革视觉（`public/css/style.css` + `tools/build-leather.js`）**

概念叫"蓝图缝线"：靛蓝皮革桌垫 + 压进皮里的蓝图线格 + 象牙色粗粒植鞣革卡片 + 烫金缝线。

- 页面底：靛蓝细粒皮；线格不再是画上去的白线，而是**压痕**（每条线 = 一道暗槽 + 紧贴的一道高光），每 5 格一条主线，每个交点一个针孔（缝纫打孔定位的意象）；四角暗角。
- 文章卡片/列表卡片：象牙色粗粒皮 + 一圈等距烫金缝线 + 边油（边缘比面深）+ 旧化晕影；置顶标记改成燕尾书签丝带；代码块变成"嵌在皮里的深色小牛皮片"；文末有一枚盲压落款（同一份签名路径，只压凹痕不上色）。
- **底纹是离线生成的静态图**，不是 CSS/SVG 滤镜：`npm run leather` 用细胞噪声（每个粒面一个细胞，交界处是褶皱）→ 高度图 → 光照，得到真实的粒面；四边无缝。产物 `public/img/leather-navy-v1.webp`、`leather-cream-v1.webp`（各约 60KB）。

### 坑 / 判断依据

- **为什么不用 feTurbulence**：各浏览器栅格化不一致，长文章滚动会掉帧，而且它给的是"脏"不是"粒"。
- **卡片底纹上盖了一层 34% 的奶油色**：第一版粒面反差太高，正文压在上面很累。改粒面视觉时先在真实正文上看可读性，别只看纹理好不好看。
- **改了底纹配方要换文件名**（`-v1` → `-v2`）：静态资源缓存 7 天，`style.css` 里的图片 URL 没法带 `?v=`。
- **`/admin/preview` 单独限流**：全站限流是 120 次/分钟，实时预览会把它吃光，连带保存按钮一起被限流。所以 `globalLimiter` 跳过它，由 `previewLimiter`（240 次/分钟，先于登录校验）兜底。
- **中文输入法**：所有键盘拦截都先判断 `isComposing` / `keyCode 229`，否则回车会在确认候选词的同时被当成"续列表"，吃掉正在打的字。
- **历史文章的 `content_html` 是保存时渲染好的**：新语法（高亮、提示框、锚点）对老文章要重新保存一次才生效。
- **CSS 里的 `[hidden]`**：元素自己写了 `display` 会盖掉浏览器默认的 `[hidden]{display:none}`，`admin.css` 末尾加了全局兜底。
- 字体（Fraunces 等）来自 Google Fonts，我这边的测试环境访问不到，所以**中文/英文字体的最终观感没在我这边确认过**，部署后请看一眼。

### 验证过什么

- 用 Playwright 驱动真实的 `editor.ejs` + 真实的 `renderMarkdown` 跑了 40 多项：每个工具栏命令（含取消）、粗体套斜体、标题互转、列表互转、智能回车/Tab、块前后空行、Ctrl+Z 能撤回工具栏操作、预览渲染、抽屉插入、大纲跳过代码块、草稿恢复、粘贴图片占位符、保存后清暂存、专注模式。全部通过。
- 前台用真实模板 + 真实 CSS 截图看过文章页和列表页。
- **没有验证**：真实数据库、真实 session/CSRF 链路（测试桩里绕过了）、Safari/Firefox、真机手机。`/admin/preview` 的 CSRF 走的是和 `/admin/posts` 相同的表单字段 `_csrf`，理论上一致，但请部署后实际点一次预览。

### 部署

```bash
git pull && npm install && pm2 restart blog-blue
```

没有数据库迁移，没有新依赖。

### 还差什么（没做）

- 文章修订历史（每次保存留一份，能回滚）——需要新表 + 迁移，这次没动 schema
- 代码块语法高亮（需要加依赖；现在只有 `language-xx` 的 class）
- 文章目录（TOC）——锚点 id 已经有了，只差前台渲染
- 皮革底纹目前只有前台；后台 UI 仍是纯色（后台追求好用，没上质感）

---

## v2 · 2026-09-19 · 品牌签名标识 + 图片水印防伪

### 背景（founder 的原始诉求）

1. 水印没贴正
2. logo（词标 + 水印）重新设计，只要 "blog.blue"，换成艺术签名字体，要专属、不要烂大街
3. 以防伪专家的视角给水印加防伪措施和艺术感，但**不要明显**

### 做了什么

**1. `src/utils/brand.js`（新增，整个品牌标识的唯一真源）**

以前页眉词标、favicon、图片水印是三份各自手写的 SVG，改一处忘两处。现在三者都从这一个文件生成。

字形：整个 "blog.blue" 不是任何一款字体，是手工拟合的**单线条连笔签名**，全部由三次贝塞尔曲线构成（`GLYPHS` 数组，一条路径一个字母；`FLOURISH` 是收尾甩出去再横扫回来的那一笔）。

为什么不用字体：
- 专属性——别人装不上"同一款字体"就复刻不了
- 服务端渲染水印时不依赖服务器装了什么字体（旧版在服务器上退化成 DejaVu Serif，很难看）
- 手写曲线的控制点是不规则的，肉眼描摹必然走形——这本身就是第一道防伪

字形调过很多版，踩过的坑记一下，免得下次白走：
- 纯连笔草书的 `b` 会被读成 `l` + `e`（"blog" 看起来像 "lelog"）。最终方案是**混合体**：上伸部走细长环（签名感），碗形做成闭合的印刷体碗（可读性）。改 `b` 的时候务必渲染出图看一眼，不要只改数字。
- `o` / `g` 收笔处如果让路径折回，渲染出来会有个小疙瘩。现在是收笔到 x 高度就停，出锋另起一笔。

**2. 四层防伪（`brand.js` 里的 `guilloche` / `rosette` / `tickCode` / `microtext`）**

设计原则：真正管用的防伪从来不是"加个显眼的封条"，而是几层彼此独立、单独看都像装饰、合起来才能判真伪的细节。

| 层 | 是什么 | 正常观看时 | 怎么查 |
|---|---|---|---|
| L1 | 手工曲线字形 | 就是个签名 | 逐笔比曲线，描摹的必走形 |
| L2 | 扭索纹 guilloché | 纸面上一点织纹（16% 不透明度） | 放大看是连续的外摆线；R/rr/d 三个参数写死在代码里，从图片反推不出来 |
| L3 | 微缩文字 | 一条发丝线 | 放大原图 300% 以上能读出重复的 "blog.blue" |
| L4 | 刻度暗码 | 蓝图上的一段比例尺 | 基线下 16 个刻度，长=1 短=0，编码每张图唯一的序列号 |
| L5 | 陷阱 | 看不出来 | 微缩文字里每 4 个循环节有一处"错字"，位置由序列号决定 |

L5 是专门对付翻印的：对方照着"看起来像 blog.blue 的一行小字"重排，只会得到一行干净的正确文本——**没有错字反而说明是假的**。

**关键实现细节（改动时最容易踩的坑）**：L3/L4 的尺寸是按**输出像素**反推的，不是按设计坐标系。`u = box.w / width` 表示一个输出像素相当于多少设计单位，微缩文字字号 = `4.0 * u`。这样水印无论缩放到多大，微缩文字在最终位图里始终落在 4px 上下——太小会被栅格化成一团灰（防伪层直接失效），太大就不叫微缩文字了。**如果以后改水印尺寸逻辑，必须重新验证微缩文字在实际输出图上还能不能读出来。**

序列号 = `HMAC-SHA256(WATERMARK_SECRET, 文件名)`，**不入库、不存表**。只要 secret 没变，任何时候拿原图都能重算。上传时会打进 pm2 日志（`[watermark] <文件名> 序列号 XXXXXX`）。

**3. 水印贴图逻辑（`src/utils/watermark.js` 重写）**

"水印没贴正"的根因：旧版画布是写死的 402×160 圆角矩形，字形只占左边一半，右边空出一大片，看起来就是贴歪了。

修法：
- 按内容真实包围盒 + 四边等量留白（PAD=22）反推 viewBox，**居中是算出来的，不是眼估的**
- 贴图从 `gravity: 'southeast'` 改成显式 `left`/`top`，留白按图片短边算（竖图横图看起来才是同一个留白）
- 位置可配：`WATERMARK_POSITION` = southeast(默认)/southwest/northeast/northwest/center
- 去掉了那个半透明圆角底板——它既是"贴歪"观感的来源，也是最容易被干净裁掉的东西

新增**自适应配色**：贴之前先用 `sharp().extract().stats()` 量落点区域亮度，亮度 > 0.55 用墨色笔画 + 浅色底衬，否则白色笔画 + 深色底衬。固定白色在雪景/白墙/米色纸面上会直接消失，**水印看不见等于防伪层也一起没了**。

底衬用"同路径描粗一遍"而不是 SVG 滤镜——服务端 librsvg 对滤镜的支持比对描边脆弱得多。

尺寸规则：宽度 = 图宽 × 26%，下限 150px（实测微缩文字还能读出来的最小尺寸），上限 320px。图太小（水印宽 > 图宽 55% 或高 > 图高 40%）直接跳过不打。GIF 仍然不打（sharp 合成会把动图压成单帧）。

**4. 查证工具 `tools/verify-watermark.js`**

```bash
npm run wm:verify -- <文件名>                    # 打印序列号 / 刻度码 / 陷阱位置
npm run wm:verify -- <文件名> --png /tmp/ref.png  # 再生成一张对照水印图
```

必须在服务器上跑（要读 `.env` 里的 secret）。序列号的依据是**文件名**不是路径，所以拿着从网上下载回来的图（只要文件名没被改）也能算。

**5. 其他连带改动**

- `public/favicon.svg`：改成签名里的 `b` 单字 + 域名点，笔画比签名本体粗一档（16px 下细节会糊）
- `public/img/og-default.png`：用新签名重新生成（1200×630，蓝图网格 + 图框角标）
- `public/css/style.css`：词标颜色不再在 CSS 里重复定义（服务端生成时已写在属性上，两处定义必然不同步）；hover 时收尾那一笔会重新描一遍（`stroke-dasharray` 动画，`prefers-reduced-motion` 下不跑）
- `src/app.js`：新增 `res.locals.signatureMark(opts)` 助手（EJS 里没法 require）
- `views/partials/wordmark.ejs`：从 30 行手写 SVG 变成一行调用

### 环境变量（新增两个）

```
WATERMARK_SECRET=<openssl rand -hex 32>
WATERMARK_POSITION=southeast
```

`deploy.sh` 已经会自动生成 `WATERMARK_SECRET`，重跑脚本时会从旧 `.env` 复用。

> ⚠️ **改了 `WATERMARK_SECRET`，此前所有已发布图片的防伪暗码就全部对不上了。** 除非确认泄露，否则不要动它。这一条比其他几个密钥的轮换优先级都低。

### 还没做 / 下一步可选

- [ ] 已经发出去的老图片不会自动补新水印。要补的话得写个批处理脚本重跑 `applyWatermark`，但老图已经带着旧水印了，会叠两层——建议要么不管，要么从原图重新传一次。
- [ ] 水印目前只在上传时打一次，写死在文件上。如果以后想做"原图存一份、对外只给带水印版"，需要改上传/静态目录的结构，现在没做。
- [ ] 微缩文字依赖服务器上的等宽字体（`deploy.sh` 装了 `fonts-dejavu-core`）。如果换到别的基础镜像，确认这个包还在，否则微缩文字会 fallback 成别的字形——不影响读，但对照图会对不上。
- [ ] 签名字形没有做中文版/单色印刷版（名片、贴纸那种场景）。需要的时候再说。

### 验证记录

- 逐版渲染出图目视比对（浅底 `#e8e2d6` / 深底 `#1a2b1f` 各测一张 1200×800）
- 微缩文字在 1200px 原图上放大 300% 可读，正常观看呈发丝线
- `npm run wm:verify` 有/无 secret 两种情况都跑通
- 词标 EJS 模板渲染通过

---

## v1 · 初始版本 ~ 2026-09

Express + PostgreSQL + EJS 的单用户博客系统。设计概念"蓝图 / Cyanotype"。
功能：Markdown 文章、标签、评论（蜜罐 + 待审核 + IP 哈希）、后台（登录/仪表盘/编辑器/设置/评论管理）、
图片上传、RSS/sitemap/SEO 结构化数据、建站时间实时计时、一键部署脚本。
安全设计详见 README"安全设计说明"一节。
